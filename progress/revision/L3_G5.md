# L3 — G5 (reescrito desde el journal: la segunda ejecución de este agente sobrescribió el fichero con un resultado degradado; esta versión es la primera aparición, más rica, tal como la devolvió la herramienta estructurada)

## Cobertura declarada

82 de 82 escenarios del lote G5 recorridos uno a uno, sin muestreo, incluidas las tres cabeceras de comentarios completas. — features/pagina_blog.feature: 29/29 escenarios (@s1–@s29) + cabecera de 158 líneas + Background. Anclas positivas correctas encontradas: @s4 (tabla de 6 destinos a mano), @s7 (tabla de 6 chips), @s18 (tabla de fronteras 200/201/400/401). Descartadas por no aplicar: @s15 «carácter por carácter el mismo que el del listado» parece tautológico pero @s3 y @s15 escriben el literal completo por separado, así que el StringLiteral que vacía el aviso muere en ambos; @s12 y @s21 son negativos pero llevan hermano positivo sobre el mismo render (6 enlaces / texto literal + enlace de vuelta). — features/reserva_chat.feature: 20/20 escenarios (@s1–@s20) + cabecera de 103 líneas. Es el fichero mejor anclado: @s1, @s4, @s11, @s12, @s18 y @s19 fijan guion, teléfonos, horario y opciones contra literales escritos a mano, y ningún escenario inyecta su propio array, así que NO hay mutante inmortal por doble inyectado. Sus recuentos son 1, 3, 4, 6 y 7 —ninguno espera 0—, así que la ceguera «0 == 0» no aplica salvo en las negaciones, que salvo @s3 tienen hermano positivo. — features/accesibilidad.feature: 33/33 escenarios (@s1–@s33) + cabecera de 128 líneas. La puerta de análisis automático SÍ tiene guarda por extractor (@s4 páginas, @s5 páginas analizadas, @s6 reglas) y el par frontera de área táctil (@s11 con 23 / @s12 con 24 exacto) mata correctamente el EqualityOperator: ambos son el modelo que el resto del lote incumple. También descarto el reverso: ninguno de los inventarios de esta feature admite el vacío como estado legítimo (el sitio siempre tiene páginas, controles y parejas de color), luego exigir la guarda es correcto; el único vacío potencialmente legítimo del lote es el de las fotografías del blog (PENDIENTE del cliente), y ahí razono en el hallazgo 8 por qué el propio @s28 lo contradice al presuponer que existen. Resultado: 21 hallazgos (6 bloqueantes, 13 graves, 2 menores).

## Hallazgos (21)

### features/pagina_blog.feature @s22 — bloqueante

- **Lente:** L3 — mutante inmortal
- **Defecto:** El tope de 3 artículos relacionados no se ejercita en ningún escenario, así que los mutantes sobre esa constante sobreviven siempre.
- **Evidencia:** And nunca se ofrecen más de 3 enlaces en ese bloque  ·  (Background) | demo-1 … | demo-2 … | demo-3 … | Medicina general | — la categoría más poblada tiene 3 artículos, luego «Sigue leyendo» nunca puede ofrecer más de 2 enlaces
- **Consecuencia:** ArithmeticOperator y EqualityOperator sobre el `slice(0, 3)` (o cualquier valor ≥ 2) producen el mismo resultado observable: el mutante sobrevive siempre y, con thresholds.break = 100 en stryker.config.json, la feature 17 NO CIERRA JAMÁS.
- **Reparación:** Ampliar el catálogo del Background con una categoría de al menos 5 artículos y añadir un escenario que exija EXACTAMENTE 3 enlaces en «Sigue leyendo» para un artículo de esa categoría.

### features/pagina_blog.feature @s27 — bloqueante

- **Lente:** L3 — verde por vacuidad (puerta sin control positivo)
- **Defecto:** La puerta anti-prototipo/anti-primera-persona son siete negaciones sobre un único extractor de texto que nunca se prueba no vacío.
- **Evidencia:** Given el visitante recorre el texto completo del listado y de los seis artículos publicados  ·  Then no aparece "Galapavet" dentro del texto de ningún artículo
- **Consecuencia:** Si el extractor devuelve la cadena vacía (contenedor equivocado, artículos no montados, OptionalChaining mutado), los siete Then pasan en verde habiendo mirado cero caracteres. La puerta que impide que la demo mienta sobre el negocio real no protege nada, y ningún mutante la delata.
- **Reparación:** Añadir el control positivo dentro del propio escenario: «And el texto recorrido contiene los seis títulos del catálogo» y «And el recuento de artículos recorridos es exactamente 6».

### features/pagina_blog.feature Background (y @s13) — bloqueante

- **Lente:** L3 — mutante inmortal por array inyectado
- **Defecto:** El contrato no dice si la tabla del Background fija la constante de producción o es el doble que se inyecta, y @s13 inyecta su propio catálogo vacío.
- **Evidencia:** Given el catálogo de demostración escrito a mano declara seis artículos, en este orden:  ·  (@s13) Given el catálogo de demostración no declara ningún artículo
- **Consecuencia:** Si es un doble —lectura natural de «escrito a mano», y es como lo usa @s13— ningún escenario evalúa nunca la constante real: ArrayDeclaration la vacía ([...] → []) y ningún test se entera. Mutante inmortal, break:100 inalcanzable. Nótese que features/accesibilidad.feature @s1 sí cierra ese agujero («el literal … no se obtiene del inventario que comprueba»); el blog no tiene ese escenario.
- **Reparación:** Añadir un ancla positiva análoga a accesibilidad @s1: «Given un literal escrito a mano con los seis identificadores y títulos … Then el catálogo de demostración del módulo contiene exactamente esos seis, en ese orden, y el literal no se obtiene del catálogo que comprueba», y declarar en el Background que @s13 y @s16 inyectan un doble.

### features/accesibilidad.feature @s30 — bloqueante

- **Lente:** L3 — verde por vacuidad (guarda por extractor)
- **Defecto:** La puerta de contraste tiene tres extractores independientes y el de texto grande no lleva ni recuento > 0 inline ni escenario de fallo cerrado propio.
- **Evidencia:** Given el inventario de parejas de color usadas para texto de al menos 24 píxeles CSS, o de al menos 18.66 píxeles CSS en negrita  ·  Then el ratio calculado de cada pareja es mayor o igual que 3   [@s29 sí lleva «And el recuento de parejas efectivamente calculadas es mayor que 0» y @s31 «And el recuento de elementos efectivamente calculados es mayor que 0»; @s30 no lleva ninguno]
- **Consecuencia:** Con el inventario de texto grande vacío el universal de @s30 es verde vacío. La propia cabecera del fichero se autoimpone la regla que aquí incumple: «la guarda va POR CADA EXTRACTOR INDEPENDIENTE, no una sola para toda la puerta». Puerta con extractores donde solo unos llevan guarda: bloqueante.
- **Reparación:** Añadir a @s30 «And el recuento de parejas efectivamente calculadas es mayor que 0» y un escenario de fallo cerrado propio para el inventario de texto grande (hermano de @s33).

### features/accesibilidad.feature @s32 — bloqueante

- **Lente:** L3 — qué mutante mata este Then
- **Defecto:** Los umbrales de contraste y la clasificación de texto grande solo se prueban por el lado que suspende: el mutante EqualityOperator que convierte >= en > sobrevive.
- **Evidencia:** Given una pareja de colores usada para texto normal cuyo ratio de contraste calculado es 4.49  ·  And el informe indica el ratio calculado 4.49 y el umbral exigido 4.5  ·  (@s30) And un texto de 23 píxeles CSS sin negrita no se clasifica como texto grande
- **Consecuencia:** `ratio >= 4.5` → `ratio > 4.5` sobrevive (4.49 suspende igual y nadie evalúa 4.5 exacto). Idéntico para el umbral 3 de @s30/@s31/@s18 y para `tamaño >= 24` y `>= 18.66` de @s30, que solo tienen los negativos 23 y 18. Cuatro mutantes supervivientes: break:100 inalcanzable. Contraste instructivo: el área táctil SÍ lo hace bien con el par @s11 (23) / @s12 (24 exacto).
- **Reparación:** Añadir los escenarios espejo: «ratio exactamente 4.5 aprueba», «ratio exactamente 3 aprueba», «texto de exactamente 24 px se clasifica como grande» y «texto de exactamente 18.66 px en negrita se clasifica como grande».

### features/accesibilidad.feature CABECERA — bloqueante

- **Lente:** L3 — la lógica queda fuera de la superficie mutable
- **Defecto:** La cabecera razona sobre mutantes ArrayDeclaration, pero ningún escenario exige que la lógica de la puerta viva en src/lib/**/*.ts o en un *-logica.ts.
- **Evidencia:** # (`[...]` → `[]`) dejaría la guarda satisfecha de forma VACUA y el mutante sería  ·  # inmortal: vaciar la lista de páginas pasaría @s4 sin que nadie note que ya no  ·  # se audita nada.   [stryker.config.json: "mutate": ["src/lib/**/*.ts", "src/**/*-logica.ts"]]
- **Consecuencia:** Tal como está escrito, la puerta puede implementarse íntegra en un script de CI o dentro del propio .test.ts, y entonces StrykerJS no genera NI UN SOLO mutante para ella: los mutantes que @s1/@s7/@s9/@s28 dicen matar no llegan a existir, y la puerta transversal de cierre del proyecto cierra con un 100 % de mutación sobre el vacío.
- **Reparación:** Añadir un escenario de ubicación —al estilo de pagina_blog @s18 y @s29, que sí están escritos como función pura— que exija que el inventario, los umbrales, las excepciones y el veredicto se consulten a un módulo puro, y dejarlo escrito en la cabecera.

### features/reserva_chat.feature CABECERA — grave

- **Lente:** L3 — la lógica queda fuera de la superficie mutable
- **Defecto:** La cabecera cita el Invariante 6 pero ningún Then lo hace falsable: los 20 escenarios se afirman sobre el DOM renderizado.
- **Evidencia:** #     Invariante 6 (la decisión vive en módulo puro; el .tsx solo cablea).
- **Consecuencia:** Avanzar el guion, cortar en la rama de urgencia, decidir si el envío está habilitado (texto.trim().length > 0), recortar espacios, componer el resumen «A · B · C» y reiniciar pueden quedarse enteros en el .tsx y satisfacer el 100 % del contrato. Con mutate limitado a src/lib/** y *-logica.ts, la feature 7 cerraría con superficie de mutación VACÍA: ConditionalExpression sobre la guarda de envío, StringLiteral sobre el separador « · » y BooleanLiteral sobre aria-disabled no se generarían nunca.
- **Reparación:** Reescribir @s7/@s10 (recorte y guarda de envío), @s11 (composición del resumen) y @s14/@s15 (corte del guion) como escenarios de función pura —entrada → salida, sin DOM—, dejando los actuales como su cableado.

### features/pagina_blog.feature @s28 — grave

- **Lente:** L3 — verde por vacuidad (puerta anti-terceros)
- **Defecto:** Los cinco Then son universales o negaciones sobre «todas las imágenes» sin ningún recuento que pruebe que se inspeccionó alguna.
- **Evidencia:** When se inspeccionan todas las imágenes del listado y del artículo  ·  And ninguna imagen apunta a "images.pexels.com" ni a ningún otro host externo
- **Consecuencia:** Con el conjunto vacío los cinco Then son verdes vacíos y la puerta anti-terceros (Decisión 9 / Invariante 3) no protege nada. Reverso considerado y descartado: el vacío PODRÍA ser legítimo (la cabecera dice «PENDIENTE: las FOTOGRAFÍAS. El cliente no publica ninguna»), pero el propio @s28 presupone que existen («el texto alternativo de la imagen del artículo describe la fotografía», «las miniaturas del bloque "Sigue leyendo" son decorativas»): el contrato afirma que hay imágenes y a la vez no las cuenta.
- **Reparación:** Añadir «And el recuento de imágenes inspeccionadas es exactamente 3 (la del artículo y las dos miniaturas de «Sigue leyendo»)», o declarar el caso de cero imágenes como escenario propio con fallo cerrado.

### features/pagina_blog.feature @s11 — grave

- **Lente:** L3 — el Then solo cuenta (0 == 0)
- **Defecto:** «no hay ningún enlace de artículo» es ciego a que el extractor de enlaces se haya roto, y ningún hermano del escenario prueba ese mismo extractor.
- **Evidencia:** Then no hay ningún enlace de artículo
- **Consecuencia:** Si el extractor de enlaces de artículo se rompe y deriva 0, 0 == 0 sigue verde y el mutante sobrevive. Los hermanos («se lee, exactamente, "No hay artículos de demostración en esta categoría."», «los 6 botones del grupo "Filtrar por categoría" siguen presentes») prueban OTRO extractor, el de los chips.
- **Reparación:** Añadir dentro del propio escenario «And el mismo recuento de enlaces de artículo vale 6 cuando el filtro vuelve a "Todos"».

### features/pagina_blog.feature @s19 — grave

- **Lente:** L3 — Then que solo cuenta ausencias, sin hermano de VALOR
- **Defecto:** En toda la feature no hay un solo Then que asevere un tiempo de lectura POSITIVO en un artículo renderizado: @s18 prueba la función pura y @s19 solo la ausencia.
- **Evidencia:** Then no se muestra ningún tiempo de lectura  ·  And en la página no aparece "0 min"
- **Consecuencia:** El cableado función→vista no está contratado: si el tiempo de lectura no se pinta NUNCA, @s19 pasa en verde y @s18 también (prueba la función, no el render). El único Then que protege el cálculo queda desconectado de la página.
- **Reparación:** Añadir a @s14 o a @s16 «And se lee el tiempo de lectura del artículo, exactamente "2 min", calculado de su cuerpo».

### features/pagina_blog.feature @s18 — grave

- **Lente:** L3 — mutante inmortal (constante escalar inyectada)
- **Defecto:** La velocidad de 200 palabras por minuto entra por el Given como dato de prueba, y ningún Then fija la constante de producción.
- **Evidencia:** Given la velocidad de lectura declarada es de 200 palabras por minuto  ·  And ningún tiempo de lectura procede de un valor escrito en el catálogo
- **Consecuencia:** Si el 200 se inyecta como parámetro, la constante de producción no se evalúa nunca y sus mutantes (ArithmeticOperator, cambio de valor) son inmortales: es el mismo defecto que el catálogo vacío, en versión escalar. La cabecera confirma que 200 es una constante del proyecto («PENDIENTE: la velocidad de lectura de 200 palabras por minuto es una constante de presentación… Si se cambia, se cambia en @s18»).
- **Reparación:** Añadir «And la velocidad de lectura declarada por el módulo es exactamente 200, escrita a mano en este escenario y no obtenida del módulo que comprueba».

### features/pagina_blog.feature @s5 — grave

- **Lente:** L3 — doble comparado contra la constante de producción (tautología)
- **Defecto:** «contiene su título» no escribe el título en el escenario: si el test lo lee del catálogo importado, ambos lados del expect se mueven juntos.
- **Evidencia:** And el nombre accesible de cada uno contiene su título y contiene "Demostración"
- **Consecuencia:** StringLiteral sobre cualquier título del catálogo de producción cambia a la vez el render y el valor esperado: el mutante sobrevive y el test es tautológico. @s4 hace lo correcto justo al lado, escribiendo los seis destinos en una tabla.
- **Reparación:** Sustituir por una tabla con los seis títulos escritos a mano, igual que la tabla de destinos de @s4.

### features/pagina_blog.feature @s6 (idéntico en @s17) — grave

- **Lente:** L3 — verde por vacuidad (escenario de solo-negativos)
- **Defecto:** Los cinco Then de @s6 son negaciones sobre el conjunto «tarjetas» y ninguno prueba que ese conjunto sea no vacío en ese render; @s17 repite el patrón sobre el artículo.
- **Evidencia:** Then ninguna tarjeta muestra un nombre de persona  ·  (@s17) Then no se muestra ningún nombre de persona
- **Consecuencia:** Rompe el extractor de tarjetas y ambos escenarios pasan mirando cero tarjetas: la regla R1 del contrato («el artículo no tiene autor»), que existe para no firmar con personas reales sin su consentimiento, queda sin protección observable.
- **Reparación:** Abrir @s6 con «Then se inspeccionan exactamente 6 tarjetas» y @s17 con «Then el artículo inspeccionado es "Artículo de demostración 3"».

### features/pagina_blog.feature @s26 — grave

- **Lente:** L3 — mutante inmortal en constante no ejercitada
- **Defecto:** El catálogo de patrones prohibidos del validador no está anclado contra un literal escrito a mano: solo se ejercitan cinco casos.
- **Evidencia:** Given un artículo de demostración por cada uno de estos textos prohibidos en su cuerpo:
- **Consecuencia:** Si el módulo declara más patrones que esos cinco (lo natural: «€», «%», patrón de teléfono, «gratis», «garantiza»), los StringLiteral de los patrones no ejercitados sobreviven y break:100 no se alcanza. ArrayDeclaration que vacíe la lista sí muere, luego la lista queda protegida en su cardinalidad pero no en su contenido.
- **Reparación:** Añadir un ancla positiva que fije la lista completa de patrones prohibidos contra un literal escrito a mano, o declarar en el contrato que la lista es exactamente esos cinco.

### features/reserva_chat.feature @s3 — grave

- **Lente:** L3 — verde por vacuidad (extractor sin control positivo)
- **Defecto:** Cuatro negaciones sobre «todo el texto visible del widget» cuyo único Then positivo se afirma sobre otro extractor (los nombres accesibles de los botones).
- **Evidencia:** When se recorre todo el texto visible del widget de reserva  ·  And el texto no contiene "Peluquería"
- **Consecuencia:** Con el texto recorrido vacío, las cuatro negaciones son verdes vacíos: la puerta que impide anunciar servicios que el cliente no presta (§7 de docs/datos-galapavet.md) no mira nada. Mismo patrón, más leve, en @s14 («And en todo el widget no aparece "24 h", ni "24h", ni "todos los días"»), allí sí con hermanos literales sobre el historial.
- **Reparación:** Añadir «And ese mismo texto recorrido contiene "Hola, soy el asistente de Galapavet"».

### features/accesibilidad.feature @s18 — grave

- **Lente:** L3 — verde por vacuidad (guarda por extractor)
- **Defecto:** @s18 consume el inventario de controles interactivos sin recuento > 0, mientras su gemelo @s16 sí lo lleva, y la guarda de fallo cerrado @s15 solo cubre la comprobación de área táctil.
- **Evidencia:** Given el inventario de controles interactivos de las seis páginas  ·  Then el área del indicador de foco de cada control es al menos la de un perímetro de 2 píxeles CSS de grosor alrededor del control sin foco   [@s16 sí lleva «And el recuento de controles a los que se dio foco es mayor que 0»; @s15 dice «When se ejecuta la comprobación de área táctil mínima»]
- **Consecuencia:** Con el inventario vacío el universal de @s18 es verde vacío y el objetivo propio SC 2.4.13 queda sin verificar. La mutación no delata el hueco: es el patrón verde-por-vacuidad-en-puerta-de-verificacion que la propia cabecera cita.
- **Reparación:** Añadir a @s18 «And el recuento de controles a los que se dio foco es mayor que 0» y generalizar el Given de @s15 a «cualquier comprobación que consuma el inventario de controles».

### features/accesibilidad.feature @s20 (idéntico en @s21) — grave

- **Lente:** L3 — verde por vacuidad (universal sin recuento)
- **Defecto:** El escenario que protege el Invariante 4 cuantifica sobre el conjunto de contenido animado sin contarlo nunca.
- **Evidencia:** Then todo el contenido que en condiciones normales aparece mediante una animación está visible desde el primer momento  ·  (@s21) Then ninguna animación se reproduce
- **Consecuencia:** Con el conjunto de elementos animados vacío, @s20 y @s21 son verdes vacíos: el estado base visible con prefers-reduced-motion queda sin proteger justo en los escenarios que existen para protegerlo. @s19 y @s22 sí llevan su recuento > 0, lo que hace la omisión asimétrica y por tanto invisible al revisor.
- **Reparación:** Añadir «And el recuento de elementos que se revelan por animación es mayor que 0» en @s20 y su equivalente en @s21.

### features/accesibilidad.feature @s27 — grave

- **Lente:** L3 — el Then solo cuenta (0 == 0)
- **Defecto:** El recuento esperado es 0 y el hermano también es un universal, así que un recorrido de tabulador que no vea nada pasa en verde.
- **Evidencia:** And el recuento de paradas dentro de los paneles colapsados es exactamente 0
- **Consecuencia:** Si el recorrido del tabulador se rompe y deriva cero paradas, 0 == 0 sigue verde y el Then hermano («el foco solo se detiene en los controles de cabecera») también es vacuo: nadie comprueba que el contenido colapsado quede realmente fuera del orden de tabulación.
- **Reparación:** Añadir «And el recuento de paradas en controles de cabecera es exactamente el número de paneles del acordeón».

### features/accesibilidad.feature @s13 (y @s14) — grave

- **Lente:** L3 — mutante superviviente en ramas no ejercitadas
- **Defecto:** La cabecera transcribe cinco excepciones normativas de SC 2.5.8 y los escenarios contratan solo dos.
- **Evidencia:** Then el veredicto para ese enlace es aprobado por la excepción «en línea»   [cabecera: «Excepciones literales: (a) Spacing …; (b) Equivalent …; (c) Inline …; (d) User Agent Control; (e) Essential.»]
- **Consecuencia:** Si el módulo declara las cinco, las ramas y los literales de Equivalent, User Agent Control y Essential no tienen ningún test: ConditionalExpression y StringLiteral sobre ellas sobreviven y break:100 no se alcanza. Si el módulo declara solo dos, la cabecera induce a implementar de más.
- **Reparación:** O un escenario por cada excepción declarada, o una frase en el contrato que diga que la implementación declara únicamente «en línea» y «espaciado» y que las otras tres no se implementan.

### features/pagina_blog.feature @s29 — menor

- **Lente:** L3 — Then que no nombra ningún mutante
- **Defecto:** El último Then asevera sobre el orden de llamadas internas, que no corresponde a ningún mutante nombrable ni es observable sin espiar la implementación.
- **Evidencia:** And en ningún punto de la página se pide un desplazamiento con "smooth" sin haber consultado antes esa preferencia
- **Consecuencia:** Ruido en el contrato: la tabla de @s29 ya mata ConditionalExpression invertida y StringLiteral sobre "auto"/"smooth"; este Then añade una obligación que el tdd_craftsman solo puede satisfacer con un espía frágil, sin ganancia de mutación.
- **Reparación:** Eliminarlo o convertirlo en una nota de la cabecera.

### features/accesibilidad.feature @s33 — menor

- **Lente:** L3 — guarda ambigua por extractor
- **Defecto:** La guarda de fallo cerrado no dice a cuál de los tres inventarios de color se aplica.
- **Evidencia:** Given un inventario de parejas de color sin ninguna entrada
- **Consecuencia:** Un implementador la satisface con una sola guarda y da por cumplida la regla «una guarda por cada extractor independiente», dejando sin cubrir los inventarios de texto grande (@s30) y de componentes de SC 1.4.11 (@s31). Es la mitad blanda del hallazgo sobre @s30.
- **Reparación:** Desdoblar @s33 en tres escenarios, uno por inventario, o nombrar el inventario concreto en el Given.

