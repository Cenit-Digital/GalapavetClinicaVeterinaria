# VEREDICTO — features/seo_estructura.feature (G1)

Verificador independiente. Fichero arbitrado: `features/seo_estructura.feature`.
Alegaciones recogidas de `L1_G1.md`, `L2_G1.md`, `L3_G1.md` (solo bloques que citan este fichero).

- **Total alegaciones recogidas:** 10
- **Fusionadas por ser el mismo defecto de fondo (duplicadosColapsados):** 1
- **Veredictos emitidos:** 9
- **CONFIRMADOS:** 6
- **REFUTADOS:** 3

## Tabla de veredictos

| Ancla | Severidad | Veredicto | Lentes originales | Motivo (cita propia) |
|---|---|---|---|---|
| @s10 (`telephone`) | grave | **CONFIRMADO** | L1, L2 | La línea 267 dice "...que el nombre accesible del enlace de teléfono visible" en singular, pero `docs/datos-galapavet.md` §2 publica **tres** teléfonos distintos con roles distintos ("Teléfono 1 **91 082 92 67**", "Teléfono 2 **685 34 31 49**", "Urgencias fuera de horario **91 851 13 93** ... rotulado literalmente «Urgencias fuera de horario»"). Ningún `Then` de todo el fichero fija cuál de los tres alimenta `telephone`, a diferencia de `streetAddress`, que @s9 sí fija letra a letra ("su calle es exactamente 'Carretera de Torrelodones, 11'", línea 250). El propio comentario de cabecera (línea 118) ya evidencia la ambigüedad al listar dos números separados por interpunto bajo el mismo campo `telephone`. Ninguna otra `Then` del fichero (@s12, línea 282, solo exige consistencia entre páginas, no corrección del valor) cierra el hueco. |
| @s10 (correspondencia de tramos) | grave | **CONFIRMADO** | L1 | La cláusula "cada tramo de horario declarado se corresponde con un tramo del horario visible, sin sobrar ni faltar ninguno" (línea 268) no puede satisfacerse por igualdad estructural simple: @s13 fija 3 tramos JSON-LD con L-V **partido en dos** entradas (líneas 294-295), mientras que `features/informacion_contacto.feature` líneas 121-126 fija el horario visible como 3 filas donde L-V es **una sola fila** ("11:00 a 14:00 y 16:30 a 20:00" en un solo texto) más una fila "Domingos" / "Cerrado" explícita. Los conteos coinciden por casualidad (3=3) pero las estructuras no: la fila de domingo visible no tiene tramo declarado equivalente (por diseño, @s14), y un tramo declarado no corresponde 1:1 a una fila visible sino a una subcadena de ella. El contrato no dice si la comparación es de cardinalidad, de subcadena, o bidireccional, y esa laguna es real y verificable contra ambos ficheros. |
| @s2 (orden del `charset`) | menor | **REFUTADO** | L1 | La ambigüedad teórica planteada (¿DOM montado en jsdom vs. HTML fuente?) no tiene ninguna divergencia práctica posible en este proyecto: `index.html` declara `<meta charset="UTF-8" />` como el segundo hijo de `<head>`, antes de `<title>` y de todo `<body>` (visto en `index.html` líneas 1-11); no hay ningún gestor dinámico de `<head>` en el repositorio (`src/` solo contiene `src/test/` en este momento, sin `react-helmet` ni equivalente), así que `<head>` precede estructuralmente a cualquier texto de `<body>` sea cual sea el método de medición. Una ambigüedad sin ningún escenario de implementación razonable que la explote no es un defecto de contrato, es ruido. |
| @s19 (cribado del reclamo de urgencias) | grave | **CONFIRMADO** | L2 | Las líneas 352-353 solo criban "24 h" y "24 horas" en título y descripción. Comparado con `features/datos_negocio.feature` @s14 línea 164, que sobre la fuente sí criba "todos los días del año" además de "24 h"/"24h", @s19 de este fichero **no** replica esa cláusula ni añade "24/7", "siempre abierto" o una cifra de años de actividad, pese a que la cabecera de este mismo fichero declara PENDIENTE justamente "años de actividad" (línea 144) y documenta que el copy heredado real decía "urgencias 24 h desde 2013". Una meta descripción que diga "abierto todos los días" o "desde 2013" pasa las 20 puertas del fichero sin que ninguna lo detecte — es una asimetría real entre el guardián a nivel de fuente y el guardián a nivel de página publicada. |
| @s5 (descripciones: Then-que-solo-cuenta) | grave (rebajado desde "bloqueante" propuesto por L3) | **CONFIRMADO** | L3 | Líneas 212-214: "obtengo exactamente cuatro descripciones" / "ninguna... es una cadena vacía ni solo espacios" / "no hay dos descripciones iguales entre sí". No hay ninguna cláusula de contenido reconocible, a diferencia de @s4 (línea 206, "cada título contiene el nombre 'Galapavet'"). Cuatro cadenas basura distintas ("a", "b", "c", "d") satisfacen el escenario completo; un mutante que baraje las descripciones reales entre páginas también sobrevive. Mantengo la severidad en "grave" y no en "bloqueante": el escenario es satisfacible y medible tal cual está, el defecto es de cobertura de mutación/contenido, no de imposibilidad de verificación. |
| @s4 (títulos: sin ancla por página) | grave | **CONFIRMADO** | L3 | Línea 206, "cada título contiene el nombre 'Galapavet'", es una subcadena que las cuatro páginas comparten por construcción (todas se llaman "Galapavet · algo"). Ningún `Then` del escenario fija, para una página concreta (p.ej. inicio), un fragmento distintivo del título de esa página en particular. Un mutante que intercambie qué título va a qué página (p.ej. el título de "tienda" se sirve en "blog") sigue cumpliendo "cuatro títulos distintos, todos contienen Galapavet" y sobrevive sin que ningún `Then` del fichero lo note. |
| @s20 (Open Graph: existencia sin valor) | grave | **CONFIRMADO** | L3 | Línea 365: "se declara un título, un tipo, una imagen y una dirección de la página" solo exige presencia de las cuatro claves, mientras la misma escena fija `og:locale` letra a letra dos líneas más abajo ("el locale declarado es exactamente 'es_ES' con guion bajo"). No hay clausula "no vacío" ni valor esperado para `og:title`/`og:type`/`og:url`, a diferencia de @s4/@s5 que sí exigen "ninguno... es una cadena vacía". Un `og:type` incorrecto (p.ej. "website" en vez del tipo correcto para un negocio local) o un `og:title` vacío pasarían la puerta si la implementación de test solo comprueba existencia de la clave. |
| @s15/@s16 (guarda de omisión: hueco de mutación LogicalOperator) | grave | **CONFIRMADO** | L3 | @s15 (línea 313) da como Given "una fuente única sin coordenadas geográficas" — ambos campos ausentes a la vez, sin variante "solo hay latitud" o "solo hay longitud". @s16 (línea 322) da como Given "una fuente única sin email... y sin perfiles de redes sociales" — de nuevo ambos ausentes a la vez, sin variante de un solo campo. Si la lógica de omisión real combina dos condiciones con un operador lógico (p.ej. "omitir geo si no hay lat Y no hay long"), un mutante que cambie ese operador (Y → O) solo se distingue cuando exactamente uno de los dos campos está presente y el otro no — caso que ningún escenario de este fichero ejercita. Confirmo que ninguna otra escena de `seo_estructura.feature` cubre ese caso intermedio (revisé @s1-@s20 completos). |
| @s8 (`@context`: sin literal exacto) | menor | **REFUTADO** | L3 | Línea 240: "el bloque declara el vocabulario de schema.org como contexto" no fija el literal exacto de `@context`. Pero, a diferencia de `telephone` (donde SÍ hay tres valores reales en conflicto en la fuente de negocio), aquí no hay ambigüedad de negocio: JSON-LD/schema.org admite variantes técnicamente válidas del mismo valor (`https://schema.org`, `http://schema.org`, con o sin barra final) — fijar un único literal sería sobre-especificar un detalle de formato del estándar, no del negocio, y podría ir en contra de la fidelidad al vocabulario que la propia cabecera investiga (líneas 36-55). La cláusula, aunque más débil que el resto del escenario, sigue siendo verificable de forma no vacía (debe contener "schema.org"); no es un defecto de contrato, es un matiz de implementación de test razonable de dejar abierto. |

## Notas de arbitraje

- La alegación de L1 y L2 sobre @s10/telephone es el mismo defecto de fondo (ambigüedad sobre cuál de los tres teléfonos alimenta la propiedad `telephone`); se fusionó en un único veredicto y se cuenta como 1 fusión en `duplicadosColapsados`.
- Ninguna alegación dependía de una cita que no pude verificar en la fuente primaria citada; todas las citas literales usadas por los agentes originales se contrastaron de nuevo contra `features/seo_estructura.feature`, `docs/datos-galapavet.md`, `features/datos_negocio.feature` e `features/informacion_contacto.feature`.
- No se investigaron ni arbitraron alegaciones de L1/L2/L3 sobre otros ficheros del grupo (`tokens_marca.feature`, `datos_negocio.feature`, `selector_paleta.feature`): quedan fuera del alcance de este veredicto.
