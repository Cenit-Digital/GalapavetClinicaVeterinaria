# VEREDICTO — features/pagina_campanas.feature (G4)

Verificador independiente. Fuentes primarias releídas: features/pagina_campanas.feature
(íntegro), docs/datos-galapavet.md (íntegro), project-spec.md (Invariantes 1-7, Decisiones
1-2), features/campanas_portada.feature (grep de literales de error), stryker.config.json,
vite.config.ts.

Alegaciones brutas sobre este fichero en L1/L2/L3: **7**. Tras colapsar 1 duplicado
(misma cabecera "duración" alegada por L1 y L3), quedan **6** veredictos: **4 CONFIRMADOS**,
**2 REFUTADOS**.

| Ancla | Severidad | Veredicto | Lentes | Motivo (cita propia) |
| --- | --- | --- | --- | --- |
| CABECERA / @s15 / @s34 / @s35 (`duracion`) | grave | **CONFIRMADO** | L1,L3 | La cabecera afirma, líneas 43-45: "`duracion` ('30 min · una sola visita') son invención. Igual tratamiento: pendiente rotulado y fallo cerrado (@s34, @s35)." Releído @s34 (:469-475) y @s35 (:477-483): solo cubren `vigencia` y `plazas`, ninguno menciona `duracion`. La tabla de @s15 (:300-303) tiene exactamente 3 filas — Precio, Vigencia, Plazas — sin fila Duración. Grep de "duraci\|30 min" en el fichero solo encuentra la propia cabecera (:43) y el Background (:157, que dice que ninguna campaña *declara* duración, pero no exige fallo cerrado si alguna lo hiciera). @s24/@s25 (las listas negras de texto) tampoco incluyen ninguna cadena de duración. El hueco es real: si el catálogo de demo incorpora `duracion: '30 min'`, ningún escenario de este fichero lo detecta, pese a que la cabecera promete explícitamente lo contrario. |
| @s5 / @s14 / CABECERA (aviso "Galapavet no publica ninguna campaña") | grave | **CONFIRMADO** | L2 | El literal exacto de @s5 (:205) y @s14 (:291) es: "Contenido de demostración. **Galapavet no publica ninguna campaña**: esta página muestra el formato…". Es una afirmación categórica sobre el negocio, no sobre la página. La única fuente que la cabecera cita para justificarla (líneas 15-18) es docs/datos-galapavet.md §7, que dice literalmente: "Campañas con precios (`49 €`, `−25 %`, `75 €`) — **NO VERIFICABLE** — no publicadas" — eso cubre solo las campañas *con precio* heredadas del prototipo, no "ninguna campaña" en general. Releí también docs/datos-galapavet.md §9 ("Verificado por ausencia"), líneas 115-127: enumera email, redes sociales, coordenadas, precios/productos de tienda y años de actividad/registro — **no** enumera campañas. La propia regla del fichero fuente (docs/datos-galapavet.md:3-5) exige "lo que se ha verificado contra una fuente primaria, con la fuente citada": aquí la cita no sostiene la amplitud de la afirmación que se publica dos veces como texto visible del sitio. |
| @s16 (`tel:+34910829267`) | menor | **REFUTADO** | L2 | project-spec.md declara, como Invariante 2 (línea 53-55): "El dato de negocio se escribe una sola vez… y **todas sus formas derivadas (`tel:`, `wa.me`, JSON-LD, texto visible) se calculan de él**." Y en "Modos de error comunes" (línea 69-70): "teléfono que no normaliza → el normalizador **lanza**… Falla cerrado." Esto confirma que la forma E.164 del `tel:` es, por diseño explícito del proyecto, un valor **calculado** a partir del dato verificado ("91 082 92 67", docs/datos-galapavet.md:27), no un segundo dato que necesite su propia cita en la fuente primaria — igual que un importe formateado no necesita que la fuente cite el símbolo de separador. No es el mismo patrón que "49 €" o "Miraflores de la Sierra" (datos inventados sin ninguna base real): aquí el número de origen sí está verificado y la transformación a `tel:+34…` es determinista y reversible. Hay lectura razonable bajo la cual esto no es un defecto de contrato, así que no lo confirmo pese a que la observación literal (que "+34" no aparece en docs/datos-galapavet.md) es correcta. |
| @s20 (imágenes, verde por vacuidad) | grave | **CONFIRMADO** | L3 | @s20 (:344-350) es el único escenario del fichero sobre imágenes. Sus tres `Then` son: "ningún origen contiene 'http://' ni 'https://'", "ningún origen contiene 'pexels'" y "cada imagen que acompaña a una tarjeta tiene el texto alternativo vacío…". Los tres son universales sobre un conjunto no acotado a un tamaño mínimo: con cero imágenes en el DOM (por ejemplo, si el componente deja de renderizar `<img>` por completo) los tres pasan en verde. Grep de "imagen" en todo el fichero confirma que ningún otro escenario fija un recuento (@s3 cuenta tarjetas y encabezados, no imágenes). Un mutante o una regresión que vacíe el array de imágenes de las campañas no lo detecta ningún `Then` de este contrato. |
| @s21 / @s22 / @s23 (rama de movimiento reducido) | grave | **CONFIRMADO** | L3 | Los tres escenarios (:352-374) están redactados íntegramente en términos de interacción DOM ("el visitante pulsa el enlace…", "el desplazamiento solicitado… es/no es suavizado"), a diferencia de @s33-@s36 ("se construye el catálogo de campañas de la página"), que sí contratan la lógica de validación como operación pura. project-spec.md Invariante 6 (línea 64-65) exige que "la lógica de decisión vive en módulos puros… el `.tsx` solo cablea", y stryker.config.json declara explícitamente que StrykerJS **no muta JSX** ("los `.tsx` solo cablean, y StrykerJS no muta ni el texto ni los atributos de JSX… así que incluirlos daría una métrica engañosa"). Si la rama de tres vías (preferencia activa/inactiva/no consultable) queda como `window.matchMedia?.(...)` inline en el `.tsx`, satisface estos tres escenarios vía DOM pero ningún mutante sobre esa decisión entra en la superficie mutable (`src/lib/**/*.ts`, `src/**/*-logica.ts`) declarada en stryker.config.json. El punto 11 de la propia cabecera (:85-87) reivindica esta rama como corrección deliberada de H-09, así que el hueco de protección es real sobre algo que el fichero mismo trata como crítico. |
| CABECERA (nota de coherencia con campanas_portada.feature) | menor | **REFUTADO** | L3 | La nota (líneas 27-31) dice: "esa feature ya fija… el distintivo único 'Demostración' y los literales de error 'precio no confirmado' y 'vigencia no confirmada'. Esta feature los reutiliza sin cambiarlos." Grep de "plazas no confirmadas\|punto no publicado\|precio no confirmado\|vigencia no confirmada" en features/campanas_portada.feature devuelve **solo** "precio no confirmado" (:148) y "vigencia no confirmada" (:155) — esa feature no declara ni "plazas no confirmadas" ni "punto no publicado". La nota de coherencia es exacta en su alcance declarado: lista los literales que se **comparten sin cambios** con campanas_portada.feature, no pretende ser un inventario de todos los literales de error de este fichero. "Plazas no confirmadas" (@s35) y "punto no publicado" (@s36) son literales propios de pagina_campanas.feature que no existen en la feature hermana, así que omitirlos de la nota de coherencia no es una omisión: es la lectura correcta de qué se comparte y qué no. |

## Resumen

- Total alegaciones brutas sobre este fichero: 7 (L1: 1, L2: 2, L3: 4).
- Duplicados colapsados: 1 (cabecera "duración", alegada independientemente por L1 y L3).
- Veredictos emitidos: 6.
- Confirmados: 4 (duración sin cobertura; aviso "no publica ninguna campaña" sin cita suficiente; imágenes verde por vacuidad en @s20; rama de movimiento reducido sin módulo puro en @s21-@s23).
- Refutados: 2 (tel: E.164 derivado — cubierto por Invariante 2 de project-spec.md; nota de coherencia con campanas_portada.feature — su alcance declarado es correcto).
