# Veredicto de arbitraje — features/pagina_tienda.feature (grupo G4)

Verificador independiente. Fuentes de alegaciones: `progress/revision/L1_G4.md` (0
alegaciones sobre este fichero — L1 revisó los 42/42 escenarios pero no reportó
defectos), `progress/revision/L2_G4.md` (5 alegaciones), `progress/revision/L3_G4.md`
(4 alegaciones). Total alegado: **9**. Duplicados colapsados: **0** (los anclas de
L2 y L3 no se solapan). Confirmados: **6**. Refutados: **3**.

| # | Ancla | Lente(s) | Severidad | Veredicto | Motivo (cita propia) |
|---|-------|----------|-----------|-----------|------------------------|
| 1 | @s14 (+ Background, @s3, @s12, @s19-@s34) | L2 | bloqueante | **CONFIRMADO** | Verifiqué el literal de `project-spec.md:82`, Decisión 1(b): «Contenido editorial y de catálogo de la demo (…): se construye como **demo rotulada como tal, sin precios ni credenciales fabricados**». El Background del fichero (`features/pagina_tienda.feature:147-156`) fija 8 importes en euros («12,50 €», «9,95 €»…) y @s14 (líneas 287-298) los repite como «Importe de ejemplo: 12,50 €». Etiquetar el campo como «importe» en vez de «precio» no cambia su naturaleza: son cifras monetarias concretas para un negocio real, exactamente lo que la Decisión 1(b) prohíbe fabricar. Confirmado también por `docs/datos-galapavet.md:127` («**Precios y productos** de la tienda» dentro de §9 «Datos que el cliente NO publica»). |
| 2 | @s3 (frente a pagina_campanas.feature @s24 y @s15) | L2 | grave | **CONFIRMADO** | Comprobé yo mismo `features/pagina_campanas.feature:380` (@s24): «Then ese texto no contiene el carácter "€"» y `features/pagina_campanas.feature:304-305` (@s15): «And el texto de esa región no contiene el carácter "€" ni el carácter "%"». Frente a esto, `features/pagina_tienda.feature:185` (@s3) solo prohíbe «%», nunca «€» — de hecho la propia @s3 exige que existan importes con «€» rotulados «de ejemplo». Dos features del mismo lote, sobre el mismo dato pendiente (§9), aplican reglas opuestas. El desacuerdo es real y no depende de ninguna lectura caritativa. |
| 3 | @s33 | L2 | grave | **CONFIRMADO** | Escaneé programáticamente las 62 apariciones de «€» en el fichero: el carácter inmediatamente anterior es U+0020 (espacio ASCII) en las 60 que preceden a un importe, y U+00A0 en **cero** casos. @s33 (línea 493) exige exactamente lo contrario: «el carácter que separa la cifra del símbolo "€" es el espacio duro U+00A0, no el espacio ASCII». La propia tabla del escenario (línea 490: `1237,50 €`) usa el espacio que su propio Then prohíbe. Esto es un defecto verificable a nivel de bytes, no una opinión de estilo. |
| 4 | @s41 (mismo literal en @s18 y @s30) | L2 | menor | **REFUTADO** | Hay una lectura razonable bajo la cual esto no es un defecto: @s41 (línea 566) fija el destino en «/#contacto» y exige explícitamente «activarlo no provoca ninguna petición de red» — es un enlace de navegación a la sección de contacto, no una función de inventario. Invitar a «consultar disponibilidad en la clínica» (es decir, llamar o visitar) no es una afirmación verificable sobre el negocio del tipo que Decisión 1(a) regula; es una CTA genérica de contacto. Además la propia cabecera del fichero (líneas 127-130, sección PENDIENTE) documenta esta elección como deliberada: «Hoy la única salida es consultar disponibilidad en la clínica por los canales reales». No es un descuido, es la respuesta ya razonada a ese pendiente. |
| 5 | @s4 | L2 | menor | **REFUTADO** | El defecto ya está cubierto por otras features del mismo contrato, que la alegación no consultó. Comprobé `features/cabecera_y_navegacion.feature:204,212` («And en la cabecera no aparece el texto "Veterinaria La Sierra"», «And el texto de la cabecera no contiene "640 22 11 90"») y `features/pie_de_pagina.feature:184,197-198` («no aparece "Veterinaria La Sierra", ni "Miraflores"…», «no aparece "918 44 21 60", ni "640 22 11 90"…»). Esos dos componentes son compartidos por TODAS las páginas, incluida la tienda, y la propia cabecera de `pagina_tienda.feature` (línea 136-138, «FUERA DE ALCANCE») declara explícitamente que cabecera y pie no pertenecen a este contrato. Añadir la guarda otra vez en @s4 sería redundante, no una corrección de un hueco real. |
| 6 | @s5 | L3 | bloqueante | **CONFIRMADO** | Releí @s5 completo (`features/pagina_tienda.feature:196-201`): las tres aserciones son negativas («ningún origen contiene "http://" ni "https://"», «ningún origen contiene "pexels"», «cada imagen… tiene texto alternativo vacío»). Busqué en todo el fichero (`grep -n -i "imagen"`) cualquier aserción positiva de cantidad de imágenes y no existe ninguna, ni en @s5 ni en ningún otro escenario (@s8 cuenta tarjetas, no imágenes). Un conjunto vacío de imágenes satisface las tres líneas de @s5 vacuamente. Es un hueco real de verificación, no cubierto en ningún otro punto del fichero. |
| 7 | @s27 | L3 | grave | **CONFIRMADO** | Comparé @s27 (línea 419: `Given la cesta contiene 99 unidades…` / `When el visitante pulsa el control…`, ambos a nivel de interacción de UI) con @s34-@s36 (líneas 496-520: `Given un estado de cesta con…` / `When se fija a 0 la cantidad…`, formulados como operaciones puras sobre estado) y con @s33 (formulado explícitamente como función pura de formateo). La propia disciplina que el fichero se impone a sí mismo en @s32-@s38 —forzar que el `tdd_craftsman` extraiga la decisión a un módulo puro— no se aplica al tope de 99 unidades. `stryker.config.json` confirma que el objetivo de mutación es solo `src/lib/**/*.ts` y `*-logica.ts`, excluyendo `.tsx` explícitamente por comentario del propio fichero de configuración. Si el tope queda cableado en el `.tsx`, Stryker no genera mutante ahí y la métrica de mutación se declara sana sin haber probado el límite. Es una inconsistencia interna real del propio fichero, no una exigencia genérica ajena a él. |
| 8 | Background (mutante inmortal, no verificado) | L3 | menor | **REFUTADO** | La propia alegación se autocalifica de «no verificado» y «riesgo arquitectónico no confirmado». Hay una lectura razonable que resuelve la ambigüedad sin necesidad de especificar mecánica de dobles de prueba: los escenarios que NO sobrescriben el Background (@s6, @s8, @s13, @s14) ejercitan el catálogo tal como lo declara el Background; los que sí necesitan un caso límite lo hacen con un `Given` explícito y distinto («Given el catálogo de demostración no declara ningún producto de la categoría "Juegos"», línea 243; «Given uno de los ocho productos… tiene el nombre vacío», línea 302). Ese patrón —Background = caso base, `Given` explícito = override puntual— es la convención estándar de Gherkin/BDD y no es un defecto específico de este fichero: se aplicaría por igual a cualquier Background de cualquier feature del proyecto, así que no es un hallazgo accionable sobre `pagina_tienda.feature` en particular. |
| 9 | @s23 | L3 | menor | **CONFIRMADO** | Verifiqué @s23 (`features/pagina_tienda.feature:378-384`): es un escenario de interacción de UI (`Given la cesta está vacía` / `When el visitante pulsa el botón de añadir…`), sin ningún escenario paralelo que ejercite una función pura `formatearContadorCesta(n)` para n=0,1,2+, a diferencia de @s33 que sí formula el formateo de importes como función pura con tabla de casos. Igual que en el hallazgo 7, esto es una inconsistencia real dentro del propio fichero (el mismo patrón se aplicó a importes y no a la singularización), y con `.tsx` fuera del objetivo de mutación de `stryker.config.json`, la pluralización podría cablearse sin quedar bajo la métrica de mutación. Severidad menor porque el riesgo funcional lo sigue cubriendo @s23 en ejecución normal; lo que se pierde es solo la señal de mutación. |

## Resumen

- **Total alegado:** 9
- **Confirmados:** 6 (#1 bloqueante, #2 grave, #3 grave, #6 bloqueante, #7 grave, #9 menor)
- **Refutados:** 3 (#4 menor, #5 menor, #8 menor)
- **Duplicados colapsados:** 0
