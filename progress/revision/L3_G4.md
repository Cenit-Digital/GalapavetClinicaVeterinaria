# L3 — G4

## Cobertura declarada

features/pagina_tienda.feature: 42/42 escenarios (@s1–@s42) leídos íntegramente, más cabecera de comentarios completa (líneas 1-139) y Background. features/pagina_campanas.feature: 39/39 escenarios (@s1–@s39) leídos íntegramente, más cabecera completa (líneas 1-148) y Background. Total 81/81 escenarios, sin muestreo. No se revisó features/campanas_portada.feature (otro lote); solo se contrastó lo que la cabecera de pagina_campanas.feature afirma sobre esa relación.

## Hallazgos (8)

### features/pagina_campanas.feature CABECERA / @s15 / @s34 / @s35 — bloqueante

- **Lente:** 3.4 puerta sin guarda por extractor
- **Defecto:** El campo `duracion` se anuncia en la cabecera con "igual tratamiento: pendiente rotulado y fallo cerrado" que vigencia/plazas, pero ningún escenario lo protege, y el panel de datos pendientes (@s15) no lo lista.
- **Evidencia:** y `duracion` ('30 min · una sola visita') son invención. Igual tratamiento: pendiente rotulado y fallo cerrado (@s34, @s35).
- **Consecuencia:** Si una campaña de producción declara `duracion` con un valor real, ningún test lo detecta: es una afirmación de negocio no verificada que puede publicarse sin que ningún escenario ni mutante lo note.
- **Reparación:** Añadir un escenario gemelo de @s34/@s35 para duración (mensaje "duración no confirmada") y añadir la fila | Duración | Pendiente de confirmar con la clínica | a @s15.

### features/pagina_tienda.feature @s5 — bloqueante

- **Lente:** 3.4 verde por vacuidad
- **Defecto:** El extractor de todas las imágenes de la página nunca se ancla a un recuento no nulo; las tres aserciones son negativas/de forma, así que un conjunto vacío las pasa todas en verde.
- **Evidencia:** When el visitante lee el origen de todas las imágenes de la página / Then ningún origen contiene "http://" ni "https://"
- **Consecuencia:** Un mutante que vacíe la función que construye el src de la imagen (o que la imagen deje de renderizarse) sobrevive siempre: break:100 nunca se cierra contra él.
- **Reparación:** Añadir Then la rejilla contiene exactamente 8 imágenes de producto antes de las comprobaciones negativas.

### features/pagina_campanas.feature @s20 — bloqueante

- **Lente:** 3.4 verde por vacuidad
- **Defecto:** Mismo patrón que en tienda @s5: el extractor de orígenes de imagen del contenido principal no se ancla a un recuento no nulo.
- **Evidencia:** When se leen los orígenes de todas las imágenes del contenido principal / Then ningún origen contiene "http://" ni "https://"
- **Consecuencia:** El mutante que vacía o rompe la fuente de la imagen de cada tarjeta no tiene ningún Then que dependa de que existan imágenes, así que sobrevive siempre.
- **Reparación:** Añadir Then el contenido principal contiene exactamente 3 imágenes de campaña antes de los negativos.

### features/pagina_tienda.feature @s27 — grave

- **Lente:** 3.5 decisión que puede quedar en el .tsx
- **Defecto:** El tope de 99 unidades por línea solo se contrata vía interacción de UI, a diferencia de @s34-@s36 que están redactados como operaciones puras sobre el estado de cesta.
- **Evidencia:** Then la cantidad mostrada en esa línea sigue siendo exactamente "99"
- **Consecuencia:** El tope puede vivir en un onClick del .tsx; un ConditionalExpression que cambie < por <= (permitiendo 100 unidades) no aparece como mutante y el break:100 del módulo de cesta se cierra sin haber protegido el tope.
- **Reparación:** Añadir un escenario puro paralelo a @s34-@s36: se incrementa en una unidad la cantidad de una línea que ya tiene 99 unidades / la cantidad resultante sigue siendo 99.

### features/pagina_campanas.feature @s21 / @s22 / @s23 — grave

- **Lente:** 3.5 decisión que puede quedar en el .tsx
- **Defecto:** La rama de tres vías del desplazamiento suave (preferencia activa/inactiva/no consultable, con fallo cerrado) se contrata íntegramente en términos de interacción DOM, sin forzar un módulo puro testeable.
- **Evidencia:** Given un entorno en el que la consulta de la preferencia de movimiento no está disponible
- **Consecuencia:** Si el fallback usa window.matchMedia?.(...) directamente en el .tsx, un mutante OptionalChaining o LogicalOperator que invierta el fallo cerrado no es mutado por Stryker y el criterio del punto 11 de la cabecera queda sin protección real de mutación.
- **Reparación:** Extraer la decisión a un módulo puro y añadir un escenario que la ejercite directamente con los tres valores de entrada, sin pasar por el DOM.

### features/pagina_tienda.feature CABECERA (Background) — menor

- **Lente:** 3.3 mutante inmortal (no verificado)
- **Defecto:** El Background usa 'declara' para fijar el catálogo, y varios escenarios lo sobrescriben explícitamente, sugiriendo inyección de datos de prueba; no está claro si algún escenario ejercita la constante de producción real sin ningún doble inyectado.
- **Evidencia:** Given el catálogo de demostración declara ocho productos repartidos en las cuatro categorías que Galapavet publica:
- **Consecuencia:** Riesgo arquitectónico no confirmado: si todos los escenarios pasan por inyección, un ArrayDeclaration que vacíe la constante real de producción podría sobrevivir sin que ningún test lo note.
- **Reparación:** Documentar explícitamente que, salvo mención contraria, los escenarios ejercitan el catálogo real de producción sin doble inyectado, o especificar el mecanismo de inyección esperado.

### features/pagina_tienda.feature @s23 — menor

- **Lente:** 3.5 decisión que puede quedar en el .tsx
- **Defecto:** La singularización '1 artículo'/'N artículos' solo se contrata mediante interacción de UI; no hay escenario que ejercite la función de formato del contador como operación pura, a diferencia de @s33 para el importe.
- **Evidencia:** And ese texto está en singular: ni la región ni el botón dicen "1 artículos"
- **Consecuencia:** Si la condición n === 1 vive en el .tsx, un EqualityOperator que la invierta no se muta y el defecto de pluralización queda invisible a la métrica de mutación aunque el test funcional lo detecte en ejecución normal.
- **Reparación:** Extraer formatearContadorCesta(n) a un módulo puro con un escenario dedicado que pruebe 0, 1 y 2+.

### features/pagina_campanas.feature CABECERA — menor

- **Lente:** documentación
- **Defecto:** El bloque de coherencia con campanas_portada.feature dice reutilizar solo dos literales de error (precio, vigencia) y omite 'plazas no confirmadas' (@s35) y 'punto no publicado' (@s36), que sí existen en este mismo fichero.
- **Evidencia:** y el distintivo único "Demostración" y los literales de error "precio no confirmado" y "vigencia no confirmada". Esta feature los reutiliza sin cambiarlos.
- **Consecuencia:** Riesgo de inconsistencia si campanas_portada.feature (fuera de este lote) declara un literal de 'plazas' distinto sin que nadie lo note aquí; bajo impacto directo en mutación porque @s35/@s36 sí existen.
- **Reparación:** Actualizar la frase de coherencia para listar los literales de error compartidos completos.

