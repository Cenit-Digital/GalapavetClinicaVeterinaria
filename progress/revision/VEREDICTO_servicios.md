# VEREDICTO — features/servicios.feature (G3)

Verificador independiente. Arbitraje de las alegaciones de L1, L2 y L3 sobre
`features/servicios.feature`. L2 no alegó nada sobre este fichero (0
hallazgos declarados en `progress/revision/L2_G3.md`).

Total alegaciones recibidas sobre este fichero: **8** (L1: 5, L2: 0, L3: 3).
Duplicados colapsados: **0** (todas las alegaciones, aunque comparten ancla en
dos casos, versan sobre defectos de fondo distintos; se listan por separado).

Confirmadas: **6**. Refutadas: **2**.

## Tabla de veredictos

| Ancla | Severidad | Veredicto | Lente(s) | Motivo (cita propia) |
| --- | --- | --- | --- | --- |
| @s2 (servicios.feature:105) | grave | **CONFIRMADO** | L1 | `servicios.feature:105` — «And ningún punto de desglose es consultable en el árbol de accesibilidad» — no se acota a la sección/tarjeta de Servicios, y el Background de este mismo fichero (`servicios.feature:85`, «Given el visitante ha abierto la landing de Galapavet») monta la landing completa, no solo la sección Servicios. `campanas_portada.feature:78` (Background) declara literalmente tarjetas tituladas **"Vacunaciones"** y **"Chequeo"**, que son exactamente dos de los puntos del desglose de «Medicina general» en `servicios.feature:147` y `servicios.feature:149`. Una consulta del árbol de accesibilidad no acotada a la tarjeta de Servicios puede confundir el título de campaña (siempre visible) con el punto de desglose (que debe estar ausente), dando falsa alarma sobre una implementación correcta. Rebajo de "bloqueante" a "grave" y corrijo el ancla de L1: `@s12` (`servicios.feature:202`, «ningún punto **de las otras 4 tarjetas** es consultable…») ya acota explícitamente a las tarjetas de Servicios y no comparte este defecto — L1 lo incluyó por error. |
| @s19 (servicios.feature:259-261) | grave | **CONFIRMADO** | L1 | `servicios.feature:261` — «cada imagen de la sección tiene texto alternativo que describe la fotografía, no un servicio» — es un juicio semántico sin procedimiento determinista de verificación, y hoy se ejercita sobre un conjunto vacío: la propia cabecera del fichero dice en `servicios.feature:68`, «PENDIENTE: fotografías de la sección. El cliente no publica ninguna». Las tres líneas de `@s19` son hoy vacuamente ciertas (0 imágenes), y la tercera línea seguirá sin ser mensurable incluso cuando existan imágenes, porque «describe la fotografía, no un servicio» no admite una comparación literal. |
| @s18 (servicios.feature:253) | grave | **CONFIRMADO** | L1 | `servicios.feature:253` — «el texto de la sección no contiene ninguna promesa de plazo ni cifra de volumen» — rompe el patrón de lista negra literal que usa el resto del mismo escenario (`servicios.feature:245-252`, ocho líneas «no contiene "<literal exacto>"»). «Promesa de plazo» y «cifra de volumen» son categorías de mundo abierto: ningún literal cierra el conjunto de frases que las violarían, a diferencia de las ocho líneas hermanas. |
| @s2, @s3, @s10 (rótulo visible) | grave | **REFUTADO** | L1 | La alegación dice que «rótulo visible» es indistinguible del «nombre accesible» bajo `test.css:false`. No lo es: `vite.config.ts:49` fija `css: false` porque «Vitest no procesa CSS por defecto: los CSS Modules devuelven un proxy» — esto desactiva el *styling* de CSS Modules, no el contenido textual del DOM ni los atributos ARIA, que jsdom soporta con normalidad. El «rótulo visible» se mide por `textContent` del botón (no depende de ninguna clase CSS), y el «nombre accesible» se mide por el cómputo de nombre accesible (`aria-label` si existe, si no el `textContent`). La propia cabecera del fichero, punto 6 (`servicios.feature:47-50`), ya distingue ambos observables explícitamente: «Se exige que el nombre accesible... identifique su bloque... El RÓTULO VISIBLE no cambia». Es una técnica estándar y bien soportada (Testing Library distingue nombre accesible de contenido de texto), no una ambigüedad del contrato. |
| @s16, Given (servicios.feature:229) | grave | **REFUTADO** | L1 | La alegación dice que el Given admite dos lecturas (sustituir el 6º punto real por blanco, o añadir un 7º punto en blanco), y que solo una es consistente. Es cierto que solo una es consistente, pero eso no es ambigüedad: `docs/datos-galapavet.md §5` fija que «Análisis» tiene exactamente **6** puntos reales, hecho que el propio fichero confirma en `servicios.feature:156` («Then la tarjeta "Análisis" muestra una lista con exactamente 6 elementos», @s7). Si se añadiera un 7º punto en blanco a esos 6 reales, el filtrado dejaría **6** supervivientes, no 5 — incompatible con el Then de `@s16` («exactamente 5 elementos», `servicios.feature:231`). Solo la lectura «se sustituye el 6º punto real por una cadena en blanco» es aritméticamente compatible con el propio Then del mismo escenario, así que un implementador que lea Given+Then juntos no tiene margen real para la lectura alternativa. |
| @s16, Then (servicios.feature:231-232) | grave | **CONFIRMADO** | L3 | `servicios.feature:231-232` — «Then la tarjeta "Análisis" muestra una lista con exactamente 5 elementos / And ningún elemento de esa lista tiene el nombre accesible vacío» — solo cuenta y niega vacío; no fija cuáles son los 5 puntos supervivientes ni su orden, a diferencia de `@s4`-`@s8` que sí tabulan el contenido literal completo de cada bloque. Un mutante que reordene, duplique o sustituya uno de los 5 puntos reales de «Análisis» (manteniendo el recuento en 5 y ninguno vacío) sobrevive a este escenario sin que nada lo detecte. Este es un defecto distinto del alegado por L1 sobre el mismo ancla (ese versaba sobre el Given, no sobre esta debilidad del Then), así que no se colapsa con la fila anterior. |
| @s18 (servicios.feature:254) | grave | **CONFIRMADO** | L3 | `servicios.feature:254` — «los únicos puntos listados son los 26 publicados en docs/datos-galapavet.md §5» — remite a un documento externo en vez de fijar una comprobación de conjunto positiva contra las tablas literales que el propio fichero ya fija en `@s4`-`@s8` (que en conjunto suman exactamente 7+4+5+6+4 = 26 puntos, verificado contra `docs/datos-galapavet.md` §5). Un extractor con duplicados entre bloques o puntos mezclados que aun así totalice 26 elementos pasaría este Then; y si el step definition compara contra la misma constante de producción de la que derivan tanto el catálogo real como el número 26, el test es tautológico frente a mutantes `ArrayDeclaration`/`StringLiteral` sobre esa constante. |
| CABECERA (servicios.feature:51-53) | menor | **CONFIRMADO** | L3 | `servicios.feature:51-53` cita los casos límite como «(@s14, @s15, @s16)», pero al leer el fichero completo el `@s14` real (`servicios.feature:212-216`) es «Cada botón de la sección tiene un nombre accesible distinto» — un escenario de accesibilidad, no un caso límite de catálogo. Los tres casos límite descritos (desglose vacío / punto en blanco / catálogo vacío) son en realidad `@s15`, `@s16` y `@s17` (`servicios.feature:218`, `227`, `234`). Desfase de uno confirmado por comparación directa; es un defecto de trazabilidad de comentario, no de comportamiento contratado, de ahí la severidad menor. |

## Notas de arbitraje

- L2 no alegó nada sobre `servicios.feature` (su informe declara 0 hallazgos
  en todo el lote G3); no hay nada que arbitrar de esa lente para este
  fichero.
- Dos filas comparten ancla `@s16` (Given vs Then) porque son defectos de
  fondo distintos, señalados por lentes distintas (L1 sobre el Given, L3
  sobre el Then); no se colapsan.
- La fila `@s2`/`@s12` de L1 se confirma solo parcialmente: se corrige el
  ancla a `@s2` en solitario, porque `@s12` (línea 202) ya está correctamente
  acotado en el propio texto del fichero y no comparte el defecto alegado.
