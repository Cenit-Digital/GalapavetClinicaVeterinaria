# L1 — G5

## Cobertura declarada

82/82 escenarios revisados uno a uno, más las tres cabeceras de comentarios completas (473+289+434 líneas). pagina_blog.feature: 29/29 (@s1–@s29). reserva_chat.feature: 20/20 (@s1–@s20). accesibilidad.feature: 33/33 (@s1–@s33). Confirmado el stack real leyendo directamente vite.config.ts (test.css:false, jsdom, sin resources:'usable') y package.json (axe-core en devDependencies, sin Playwright ni ningún runner de navegador real) y harness.config.json (solo vitest + stryker declarados). Detalle sección a sección en progress/revision/L1_G5.md.

## Hallazgos (13)

### features/accesibilidad.feature @s2 — bloqueante

- **Lente:** satisfacibilidad_y_mensurabilidad
- **Defecto:** «0 violaciones» de axe-core sobre páginas construidas en jsdom con css:false no mide contraste ni área táctil real: esas reglas de axe (incluidas por el runOnly de @s7) o bien no encuentran nada que violar por falta de CSS real (pase vacío) o bien fallan para todo control por geometría 0×0 (0 violaciones se vuelve imposible)
- **Evidencia:** Then el recuento total de violaciones es exactamente 0
- **Consecuencia:** el tdd_craftsman no puede escribir un test honesto sin excluir a mano reglas de axe no autorizadas por el contrato, o el escenario pasa en verde sin haber comprobado nunca contraste/área táctil reales
- **Reparación:** documentar en el escenario o en la cabecera qué reglas de axe (color-contrast, non-text-contrast, target-size) quedan excluidas del runOnly de @s2 porque las cubren @s28-@s33 y @s9-@s15 por otra vía

### features/accesibilidad.feature @s10 — bloqueante

- **Lente:** satisfacibilidad_y_mensurabilidad
- **Defecto:** pide medir el área de contacto declarada de controles de las seis páginas reales sin especificar el mecanismo de lectura; jsdom no calcula layout (getBoundingClientRect siempre 0) y no hay ninguna aclaración equivalente a la que sí existe para los pares de color (tokens_marca)
- **Evidencia:** Given el inventario de controles interactivos de las seis páginas / When se mide el área de contacto declarada de cada control interactivo
- **Consecuencia:** el implementador tiene que inventar un mecanismo de medición (p. ej. un nuevo atributo data-*) no exigido por ningún otro escenario del contrato
- **Reparación:** convertir @s10 en evaluación de una función pura sobre valores de entrada explícitos, como ya hacen @s11-@s14, o fijar en la cabecera el módulo .ts declarado del que sale el ancho/alto de cada control real

### features/accesibilidad.feature @s16 — grave

- **Lente:** satisfacibilidad_y_mensurabilidad
- **Defecto:** mezcla una cláusula asertable (activeElement) con otra que exige un indicador de foco visualmente distinguible, dependiente de CSS renderizado que no existe con css:false, y sin ningún atributo ARIA que lo traduzca
- **Evidencia:** Then cada control expone un indicador de foco distinguible de su estado sin foco
- **Consecuencia:** la cláusula de distinguibilidad visual queda sin ningún test real posible, o se inventa un atributo de estado ad hoc no contratado en otro sitio
- **Reparación:** limitar el Then a lo asertable (activeElement, tabindex) o exigir explícitamente que el estado de foco quede reflejado en un atributo ARIA/data consultable

### features/accesibilidad.feature @s17 — bloqueante

- **Lente:** satisfacibilidad_y_mensurabilidad
- **Defecto:** depende íntegramente de geometría real (bounding rects de la cabecera fija vs. el control enfocado, posición de scroll, área visible), inexistente en jsdom
- **Evidencia:** Then el control enfocado no queda enteramente tapado por la cabecera fija / And al menos parte del control enfocado queda dentro del área visible
- **Consecuencia:** insatisfacible tal cual en Vitest+jsdom; no hay ningún Given que lo reduzca a lógica pura
- **Reparación:** reescribir como comprobación de una función pura que, a partir de la altura declarada de la cabecera y la posición declarada del control, calcule el scroll-margin/offset necesario, siguiendo el mismo patrón que pagina_blog.feature @s29

### features/accesibilidad.feature @s18 — bloqueante

- **Lente:** satisfacibilidad_y_mensurabilidad
- **Defecto:** exige área geométrica real del indicador de foco y contraste calculado entre píxeles efectivamente renderizados en dos estados, no entre dos hexadecimales declarados; ninguna de las dos magnitudes es calculable en jsdom con css:false
- **Evidencia:** Then el área del indicador de foco de cada control es al menos la de un perímetro de 2 píxeles CSS de grosor alrededor del control sin foco / And el ratio de contraste calculado entre los mismos píxeles en estado con foco y sin foco es mayor o igual que 3
- **Consecuencia:** insatisfacible sin inventar un doble no contratado; el propio PENDIENTE del fichero reconoce que ni siquiera hay un color de foco decidido todavía
- **Reparación:** anclar los colores de foco/sin-foco como constantes declaradas en un módulo .ts y reescribir el Then como cálculo de fórmula sobre esos dos hexadecimales, no sobre píxeles renderizados

### features/accesibilidad.feature @s19 — bloqueante

- **Lente:** satisfacibilidad_y_mensurabilidad
- **Defecto:** «animación en curso» depende de un motor de animaciones CSS/Web Animations que jsdom no implementa de forma fiable, y css:false no carga ningún @keyframes real; el mismo problema afecta a @s21 y @s22 y parcialmente a @s20, y a diferencia de pagina_blog.feature @s29 (que resuelve un requisito equivalente con una función pura) no hay aquí ninguna traducción a estado consultable
- **Evidencia:** Then ningún elemento de la página tiene una animación en curso
- **Consecuencia:** sin API en jsdom que interrogar honestamente, el escenario queda sin implementación posible o se resuelve con un doble (p. ej. leer una clase CSS) que el propio repositorio prohíbe expresamente
- **Reparación:** extraer a un módulo puro .ts la decisión de qué debería animarse dado prefers-reduced-motion, y reescribir @s19/@s21/@s22 (y la parte relevante de @s20) sobre el resultado de esa función, no sobre el motor de animación real

### features/accesibilidad.feature @s24 — bloqueante

- **Lente:** satisfacibilidad_y_mensurabilidad
- **Defecto:** el Given fija una precondición de breakpoint sin decir cómo se produce en el test; jsdom no resuelve @media a partir del tamaño de ventana salvo mock explícito de matchMedia, y css:false no tiene ninguna regla @media real que aplicar
- **Evidencia:** Given una página mostrada por debajo del breakpoint del menú móvil
- **Consecuencia:** un implementador honesto no tiene forma contratada de producir el estado «por debajo del breakpoint» en jsdom sin inventar un mecanismo no exigido en ningún otro punto del fichero
- **Reparación:** añadir al Given cómo se fuerza esa condición (p. ej. mockeando window.matchMedia) y documentar en la cabecera que el layout responsive se decide en JS consultable, no solo en CSS

### features/accesibilidad.feature @s29 — grave

- **Lente:** satisfacibilidad_y_mensurabilidad
- **Defecto:** el Given habla de un inventario de parejas de color «usadas... en las seis páginas» como si se extrajera del render real (irrealizable, igual que @s10); la aclaración de que en realidad viene de tokens_marca vive solo en el PENDIENTE de la cabecera, no en el propio escenario, y el mismo defecto afecta a @s30 y @s31
- **Evidencia:** Given el inventario de parejas de color usadas para texto normal en las seis páginas
- **Consecuencia:** un implementador que lea solo el escenario puede intentar derivar el inventario del DOM renderizado y chocar con la misma imposibilidad que @s10
- **Reparación:** mover la aclaración del PENDIENTE al propio Given: «el inventario de parejas de color que declara tokens_marca para texto normal en las seis páginas (inventario declarado, no extraído del render)»

### features/pagina_blog.feature @s28 — grave

- **Lente:** satisfacibilidad_y_mensurabilidad
- **Defecto:** «el texto alternativo de la imagen del artículo describe la fotografía y no afirma ningún servicio» es un juicio de contenido sobre prosa libre, sin literal ni lista de palabras prohibidas contra la que comparar, a diferencia del patrón determinista que usan @s26/@s27 de la misma feature
- **Evidencia:** And el texto alternativo de la imagen del artículo describe la fotografía y no afirma ningún servicio
- **Consecuencia:** solo la mitad de la cláusula («no afirma ningún servicio») es convertible en test real; «describe la fotografía» queda sin ningún test posible y depende de revisión editorial humana que el propio PENDIENTE de la cabecera ya reconoce fuera de alcance del arnés
- **Reparación:** separar en dos: una aserción verificable (el alt no contiene nombres de los 5 bloques de servicio ni los textos prohibidos de @s27) y marcar «describe la fotografía» explícitamente como revisión humana fuera del alcance del judge/mutation_tester

### features/pagina_blog.feature @s28 — menor

- **Lente:** satisfacibilidad_y_mensurabilidad
- **Defecto:** «la página no realiza ninguna petición automática a un tercero» es trivialmente cierta en jsdom sin mocking explícito, porque jsdom no ejecuta cargas de red reales por defecto (vite.config.ts no declara resources:'usable')
- **Evidencia:** And la página no realiza ninguna petición automática a un tercero
- **Consecuencia:** pase vacuo: no protege nada más allá de lo que ya cubre la cláusula anterior sobre el atributo src
- **Reparación:** eliminar la cláusula o sustituirla por una aserción sobre el DOM (ningún src/href apunta a un host distinto del propio sitio), que sí es observable sin mocking de red

### features/pagina_blog.feature @s5 — menor

- **Lente:** satisfacibilidad_y_mensurabilidad
- **Defecto:** «ninguna tarjeta muestra ningún otro distintivo de estado» es una categoría abierta y no enumerada, sin lista cerrada de qué cuenta como distintivo de estado más allá de «Destacado»
- **Evidencia:** And ninguna tarjeta muestra ningún otro distintivo de estado
- **Consecuencia:** el judge no tiene lista cerrada contra la que verificar exhaustividad; dos implementaciones razonables podrían discrepar
- **Reparación:** eliminar por redundante con @s6 (que ya prohíbe autor/firma/fecha) o enumerar explícitamente qué otros distintivos quedan prohibidos

### features/pagina_blog.feature @s3 — menor

- **Lente:** satisfacibilidad_y_mensurabilidad
- **Defecto:** «ese aviso es texto visible, no solo un atributo de accesibilidad» no especifica con qué API se comprueba la visibilidad bajo css:false, aunque sí existe un camino de aserción sin CSS (comparar textContent)
- **Evidencia:** And ese aviso es texto visible, no solo un atributo de accesibilidad
- **Consecuencia:** riesgo de interpretación distinta entre implementador y judge sobre qué cuenta como visible sin layout real
- **Reparación:** aclarar en el Then: «y su textContent no está vacío, a diferencia de un aviso expresado solo como aria-label»

### features/reserva_chat.feature @s20 — menor

- **Lente:** satisfacibilidad_y_mensurabilidad
- **Defecto:** «sin recurrir a su color ni a su posición» no añade ninguna aserción comprobable nueva: en jsdom con css:false el color nunca es observable de todos modos, así que la cláusula no se puede falsear ni confirmar
- **Evidencia:** And el autor de cada mensaje se puede leer en su propio texto, sin recurrir a su color ni a su posición
- **Consecuencia:** cláusula sin traducción a ningún assert nuevo, puede confundir al tdd_craftsman buscando qué comprobar que no esté ya cubierto por la línea anterior
- **Reparación:** eliminar por redundante, dado que el stack ya garantiza por construcción que ningún test puede depender de color

