# VEREDICTO — features/galeria.feature (grupo G3)

Verificador independiente. Fuentes de alegaciones: `progress/revision/L1_G3.md`,
`progress/revision/L2_G3.md` (0 hallazgos sobre este fichero), `progress/revision/L3_G3.md`.
Fuentes primarias contrastadas: `features/galeria.feature`, `docs/datos-galapavet.md`,
`project-spec.md` (invariantes, decisiones 1, 2, 8, 9), `vite.config.ts`,
`stryker.config.json`, y por referencia cruzada `features/campanas_portada.feature`,
`features/accesibilidad.feature`, `features/equipo.feature`, `features/servicios.feature`,
`features/faq.feature`.

Total alegaciones sobre este fichero: **10** (6 de L1, 0 de L2, 4 de L3).
Duplicados colapsados: **0** (ninguna pareja de lentes alegó el mismo defecto de fondo
sobre la misma ancla).
Confirmados: **8**. Refutados: **2**.

| Ancla | Severidad | Veredicto | Lente(s) | Motivo (cita propia) |
| --- | --- | --- | --- | --- |
| @s5, @s6, @s7 | bloqueante | **REFUTADO** | L1 | El Then es una relación ("ancho de tarjeta + separación"), no un literal, y el propio proyecto tiene un patrón establecido para exponer magnitudes ligadas a CSS a un módulo puro testeable: decisión 8 de `project-spec.md` cita el patrón `logica-pre-pintado-inline-se-espeja-en-gemelo-puro-testeable`. La propia cabecera de `galeria.feature:57-59` deja dicho que la separación "se re-mide en la feature tokens_marca" y que el contrato "solo exige que el paso de desplazamiento sea «ancho de tarjeta + separación efectiva», nunca un literal" — es decir, ya asume una fuente canónica futura, no un vacío. Existe lectura satisfacible: el test importa la misma constante que usa producción (Invariante 2, fuente única), sin layout CSS real en jsdom. |
| @s9, @s10 | bloqueante | **CONFIRMADO** | L1 | `galeria.feature:141` ("el desplazamiento horizontal de la pista sigue siendo 0") y `:151` ("el desplazamiento horizontal de la pista no cambia") aseveran sobre `scrollLeft` físico. jsdom no ejecuta `scrollBy`/scroll real. Como @s7 exige explícitamente desplazamiento "suavizado" (`:109`), la vía natural de implementación es `scrollBy({behavior:'smooth'})`, que en jsdom no mueve `scrollLeft` exista o no guarda correcta: la línea es verde con cualquier implementación, incluida una rota. El propio fichero demuestra la alternativa correcta en el plano de la solicitud: "no se solicita ningún desplazamiento a la pista" (`:150`), que @s9 no adopta. |
| @s14 | bloqueante | **CONFIRMADO** (reencuadrado) | L1 | Comparando `galeria.feature:185` ("cada pie de foto que cita un servicio cita uno de los cinco bloques publicados por el cliente") contra las tres líneas anteriores del mismo escenario (`:182-184`, listas negras literales tipo "ningún pie de foto menciona «peluquería»"), la cuarta línea rompe el patrón de literalidad sin dar tabla de clasificación pie→bloque: no es mecánicamente decidible. Discrepo del encuadre de "contradicción con la cabecera" que usa L1 (líneas 20-21 son ejemplo descriptivo del prototipo heredado, no un mandato de que esos pies literales sobrevivan, y el PENDIENTE de `:55-56` confirma que "el catálogo de demo se declara en la implementación"), pero el defecto de fondo —criterio no determinista— se sostiene igual. |
| @s12 | grave | **CONFIRMADO** | L1 | `galeria.feature:166` ("un aviso visible cuyo texto declara que las fotografías y sus pies son contenido de demostración") es paráfrasis. Contraste directo con el patrón hermano del mismo lote: `campanas_portada.feature:108` fija el aviso análogo como literal cerrado: "el aviso visible es exactamente «Contenido de demostración. Galapavet no publica ninguna campaña...»". `galeria.feature` no replica ese patrón pese a proteger un aviso igual de sensible (impide que fotos de pacientes inventados pasen por reales). |
| @s13 | grave | **REFUTADO** | L1 | La propia evidencia citada por L1 contiene la cláusula que refuta su lectura: `galeria.feature:176` dice "atribuye las fotografías a personas o animales concretos **atendidos por Galapavet**", no "con nombre propio". Lectura no contradictoria razonable: los pies de demo con nombres ficticios ("Nala y Coco", cabecera `:20-21`) no "atribuyen atención por Galapavet" mientras el aviso de @s12 los marque como demostración; la línea 176 cierra el flanco de afirmaciones de trato real (p. ej. "clienta de Galapavet desde 2020"), no el uso de nombres de mascota en la ficción de la demo. |
| @s4 | grave | **CONFIRMADO** | L1 | `galeria.feature:98` ("recorre la página con el tabulador hasta la sección «Galería»") es una frase única en todo el repo: `accesibilidad.feature:276` prueba foco con "cada control interactivo recibe el foco por teclado", más local y no dependiente del orden global. Acoplar el escenario al recorrido de tabulación desde el inicio de toda la landing lo hace frágil ante cambios en secciones ajenas (hero, servicios, equipo…) sin cota de pulsaciones. La objeción de L1 sobre exigir "tabindex explícito" es más discutible (el Then describe resultado, no mecanismo, que es correcto en BDD), pero la fragilidad por acoplamiento global sí es un defecto real. |
| CABECERA / @s1–@s4, @s11–@s14 | bloqueante | **CONFIRMADO** (matizado) | L3 | Verificado línea a línea: @s1(`:71`), @s2(`:80`), @s3(`:88`), @s4(`:97`), @s11(`:155`), @s12(`:164`), @s13(`:172`), @s14(`:180`) usan todos, literalmente, "Given un catálogo de galería con al menos una entrada" — nunca "el catálogo real/por defecto". Ningún escenario ejercita el array de producción sin sustituirlo. Matizo el encuadre estricto de "mutante inmortal" de Stryker: `stryker.config.json` limita `mutate` a `src/lib/**/*.ts` y `*-logica.ts`; si el catálogo vive en `src/data/galeria.ts` (arquitectura de `project-spec.md`: "`src/data/*.ts` — catálogos estáticos tipados"), Stryker no generaría mutantes ahí bajo la config actual, así que técnicamente no sería "inmortal" en sentido Stryker. Pero el defecto de fondo —ninguna protección de regresión contra un catálogo de producción vaciado o corrompido— es real y se confirma igual. |
| @s1, @s2 | grave | **CONFIRMADO** | L3 | `galeria.feature:71` ("con al menos una entrada") admite un fixture de una sola entrada; con una sola entrada, `:76` ("ninguna imagen tiene el mismo nombre accesible que otra") y `:84` ("el pie de cada figura es el de su propia entrada y no el de otra") no tienen nada que comparar y pasan vacuamente. El cuantificador "al menos" no exige mínimo 2. |
| @s1 (caso límite ausente) | grave | **CONFIRMADO** | L3 | Revisados los 15 escenarios uno a uno: ninguno prueba una entrada de catálogo con nombre o pie en blanco, a diferencia de `equipo.feature` @s9 (`:127-130`), `servicios.feature` @s16 (`:229-232`) y `faq.feature` @s12 (`:172-175`), los tres con escenario dedicado a esa entrada corrupta. `galeria.feature:75` ("el nombre accesible de cada imagen no está vacío") presupone un fixture ya válido, sin escenario que fuerce el caso límite y compruebe el filtrado. |
| @s5 (lógica atrapada en .tsx) | grave | **CONFIRMADO** | L3 | Contraste textual directo: `campanas_portada.feature:147,154` usa "se construye el modelo de la sección de campañas" para empujar la validación a una función de construcción pura; `galeria.feature:106-109` usa "el visitante pulsa el botón «Foto siguiente»" seguido de "se solicita a la pista un desplazamiento..." sin verbo que ancle el cálculo a un módulo puro. `stryker.config.json` excluye explícitamente los `.tsx` del `mutate` ("los .tsx solo cablean, y StrykerJS no muta ni el texto ni los atributos de JSX"): si la aritmética (signo, suavizado, magnitud) queda en el manejador del `.tsx`, el umbral `break:100` no la protege en absoluto pese al detalle del contrato. |

## Resumen

- **Confirmados (8):** @s9/@s10 (verde por vacuidad de `scrollLeft` en jsdom), @s14 (criterio no determinista pie→bloque), @s12 (aviso sin literal fijado, inconsistente con `campanas_portada.feature`), @s4 (acoplamiento frágil al orden global de tabulación), CABECERA/@s1-4,11-14 (ningún escenario ejercita el catálogo real de producción), @s1/@s2 (cuantificador "al menos una" vacía las comprobaciones de unicidad), @s1 (falta caso límite de entrada en blanco, inconsistente con equipo/servicios/faq), @s5 (aritmética de desplazamiento sin ancla a módulo puro, fuera del alcance de mutación de `.tsx`).
- **Refutados (2):** @s5/@s6/@s7 (la falta de literal en la separación es intencional y satisfacible vía el patrón `gemelo-puro-testeable` ya documentado en el proyecto), @s13 (la propia cita de la alegación —"atendidos por Galapavet"— contradice su propia lectura; existe interpretación no contradictoria razonable).
