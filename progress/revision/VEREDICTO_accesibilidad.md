# VEREDICTO — features/accesibilidad.feature (G5)

Verificador independiente. Arbitraje de las alegaciones de L1 (satisfacibilidad y
mensurabilidad), L2 (fidelidad a la fuente primaria) y L3 (mutación y verde por
vacuidad) sobre `features/accesibilidad.feature` únicamente.

Fuentes contrastadas: `features/accesibilidad.feature` (434 líneas, íntegro),
`docs/datos-galapavet.md` §10.1 y §9, `project-spec.md` (Invariantes 1-7),
`vite.config.ts` (`environment: 'jsdom'`, `css: false`, sin `resources:'usable'`),
`stryker.config.json` (`mutate` limitado a `src/lib/**/*.ts` y `*-logica.ts`,
`thresholds.break = 100`), `package.json` (axe-core 4.13 en devDependencies, sin
Playwright/Puppeteer/jest-axe), `features/cabecera_y_navegacion.feature` (patrón
hermano de breakpoint).

Total alegaciones sobre este fichero: **17** (L1: 8, L2: 1, L3: 8).
Confirmadas: **13**. Refutadas: **4**. Duplicados colapsados: **0** (todo par de
alegaciones que coincide en ancla, p. ej. @s18 o @s30, defiende un defecto de
fondo distinto, no una repetición).

## Tabla de veredictos

| Ancla | Severidad | Veredicto | Lente(s) | Motivo (cita propia) |
| --- | --- | --- | --- | --- |
| @s2 | bloqueante | **CONFIRMADO** | L1 | `@s7` obliga a incluir el nivel «WCAG 2.2 AA» en el `runOnly` del análisis automático, y ese tag arrastra la regla `target-size` de axe-core (2.5.8), que solo puede evaluarse con `getBoundingClientRect` real. `vite.config.ts` fija `environment: 'jsdom'` sin ningún motor de layout y `css: false`; `package.json` no trae Playwright ni un runner de navegador real, solo `axe-core` a pelo. No hay ninguna cláusula en `@s2` ni en la cabecera que excluya esa regla o aclare cómo se resuelve sin layout real. |
| @s10 | grave (rebajado de bloqueante) | **CONFIRMADO** | L1 | El Given/Then mezcla lenguaje: "se mide el área de contacto **declarada**" (When) frente a "el ancho **medido** de cada control" (Then, L221-222). Ninguna de las dos palabras se define, y a diferencia de `@s29` (cuyo PENDIENTE de cabecera, L111-115, aclara explícitamente que el inventario de color "no se extrae del render"), aquí no existe aclaración equivalente sobre si el área sale de un valor declarado en un módulo puro (Invariante 6, project-spec.md L67-68) o de una medición DOM real, imposible en jsdom. Confirmado como ambigüedad real, no como bloqueo total: Invariante 6 ya establece el patrón de resolución (módulo puro), así que un implementador diligente tiene un camino, pero el escenario no lo dice, a diferencia de su análogo de color. |
| @s16 | grave | **REFUTADO** | L1 | La propia cabecera fija la regla que L1 pasa por alto: "Todo Then de esta feature se afirma sobre: el rol accesible, el nombre accesible, los atributos ARIA, el recuento... o una MAGNITUD CALCULADA" (L98-101). `@s18`, unas líneas más abajo, es exactamente esa magnitud calculada (área + contraste del indicador de foco) aplicada al mismo hecho ("indicador de foco distinguible"). `@s16` no necesita inventar un mecanismo nuevo: reutiliza el mismo cálculo que `@s18` ya exige, con un umbral más laxo (que exista diferencia, no que alcance el umbral numérico exacto). La cláusula de `activeElement` es trivialmente asertable, como el propio L1 admite. No hay ninguna cláusula de `@s16` que quede sin traducción posible. |
| @s17 | bloqueante | **CONFIRMADO** | L1 | El Then depende de geometría real: "el control enfocado no queda enteramente tapado por la cabecera fija" y "al menos parte del control enfocado queda dentro del área visible" (L286-287) solo son evaluables con `getBoundingClientRect` de la cabecera fija, la posición de scroll y el rect del control — jsdom no calcula layout, con o sin `css:false`. Ningún Given del escenario reduce esto a lógica pura (compárese con el propio patrón que el fichero SÍ usa en `@s9` y `@s18` línea 296, anclando el umbral a un literal escrito a mano en vez de a geometría renderizada). |
| @s18 (geometría/contraste de píxeles) | bloqueante | **CONFIRMADO** | L1 | "el área del indicador de foco... al menos la de un perímetro de 2 píxeles CSS" y "el ratio de contraste calculado entre los mismos píxeles en estado con foco y sin foco" (L294-295) exigen píxeles efectivamente renderizados en dos estados. jsdom no rasteriza nada: no hay superficie de píxeles que leer, con o sin `css:false`. La propia cabecera reconoce que "el valor concreto del grosor y el color del indicador de foco no está decidido" (L116-118), pero eso solo cubre EL VALOR, no CÓMO se mide sin un motor de render real. |
| @s18 (guarda de conteo) | grave | **CONFIRMADO** | L3 | Verificado por comparación textual directa: `@s16` (L279) lleva "And el recuento de controles a los que se dio foco es mayor que 0"; `@s18` consume el mismo "inventario de controles interactivos de las seis páginas" (L292) y no lleva ninguna cláusula equivalente. Con el inventario vacío, los dos universales de `@s18` (área y contraste) son verdes vacíos y el objetivo propio SC 2.4.13 queda sin verificar, exactamente el patrón `verde-por-vacuidad-en-puerta-de-verificacion` que la propia cabecera cita como regla del fichero (L74-94). |
| @s19 | bloqueante | **CONFIRMADO** | L1 | "ningún elemento de la página tiene una animación en curso" (L306) exige interrogar un motor de animaciones CSS/Web Animations. `vite.config.ts` fija `css: false` (ninguna hoja de estilos, y por tanto ningún `@keyframes`, se aplica nunca) y `environment: 'jsdom'` sin ningún polyfill de animación en `package.json`. Ningún Given de `@s19` traduce el requisito a un estado consultable (atributo ARIA, magnitud calculada), a diferencia de lo que exige la propia cabecera del fichero (L98-101) para todo Then. |
| @s24 | bloqueante | **REFUTADO** | L1 | `features/cabecera_y_navegacion.feature` ya establece y ejercita el mecanismo para esta misma decisión de rama, con el mismo componente (menú móvil): `@s2`/`@s3` de esa feature usan "Given el ancho de la ventana es exactamente el valor declarado del punto de corte" / "...un píxel menor..." (líneas 108-118 de ese fichero) sobre un punto de corte anclado por literal en `@s1` (1024 px). `accesibilidad.feature @s24` no inventa un mecanismo nuevo: reutiliza la misma decisión de rama (móvil/escritorio) que ese fichero hermano ya deja resuelta y testeada; la redacción "por debajo del breakpoint del menú móvil" es más laxa en la letra pero no exige nada que el proyecto no sepa ya producir. |
| @s29 | grave | **REFUTADO** | L1 | La aclaración que L1 dice que falta SÍ existe, literalmente, en la cabecera: "Esta puerta no recalcula la paleta: comprueba que ninguna pareja usada en la interfaz baja de los umbrales anclados aquí" (L112-114), inmediatamente después de atribuir la lista a `tokens_marca`. Es un desacuerdo sobre UBICACIÓN del texto (cabecera vs. Given), no sobre su ausencia: el propio patrón de PENDIENTEs de este fichero (rutas, foco, AAA) usa sistemáticamente la cabecera para estas aclaraciones, y las otras 16 alegaciones arbitradas en este mismo informe citan la cabecera como fuente autorizada sin objetar el lugar. |
| @s30 (y @s31, umbral genérico) | menor | **REFUTADO** | L2 | `docs/datos-galapavet.md` §10.1 (L157, L175-176) marca el par verde/lima "No se usa" precisamente para que NUNCA entre en el "inventario de parejas... usadas en la interfaz" que `@s29`-`@s31` consumen (la cabecera, L112-114, delega esa exclusión en `tokens_marca`). Si `tokens_marca` cumple su propio contrato, el par nunca llega a evaluarse aquí, así que el umbral genérico `>= 3` no tiene ocasión de aprobarlo. El propio L2 reconoce "es menor porque tokens_marca (feature 1) es la dueña declarada de la lista": es una alegación sobre la feature 1, no sobre un defecto de `accesibilidad.feature`. |
| @s30 (guarda de conteo) | bloqueante | **CONFIRMADO** | L3 | Verificado línea a línea: `@s29` (L400) lleva "And el recuento de parejas efectivamente calculadas es mayor que 0"; `@s31` (L417) lleva "And el recuento de elementos efectivamente calculados es mayor que 0"; `@s30` (L403-409), con el mismo patrón de universal sobre un inventario ("el inventario de parejas de color usadas para texto de al menos 24 píxeles CSS..."), no lleva ninguna cláusula de recuento. Incumple la regla que la propia cabecera se autoimpone: "la guarda va POR CADA EXTRACTOR INDEPENDIENTE, no una sola para toda la puerta" (L79-80). |
| @s32 | bloqueante | **CONFIRMADO** | L3 | Verificado por ausencia: en las 434 líneas del fichero no existe ningún escenario que pruebe el lado de aprobación exacta de los umbrales de contraste (`ratio == 4.5` o `ratio == 3`) ni de los tamaños de texto grande (`24` px o `18.66` px negrita) — `@s32` solo prueba 4.49 (falla) y `@s30` solo prueba 23 y 18 (no-grande). El único par de la feature que SÍ cubre ambos lados de una frontera exacta es el área táctil, `@s11` (23, suspende) / `@s12` (24 exacto, aprueba) — la asimetría es real y deja `ratio >= X` → `ratio > X` como mutante EqualityOperator superviviente, contra `thresholds.break: 100` de `stryker.config.json`. |
| CABECERA (lógica fuera de la superficie mutable) | bloqueante | **CONFIRMADO** | L3 | Verificado por ausencia: ninguna de las 33 escenarios ni la propia cabecera contiene las cadenas "módulo puro", "-logica.ts" ni "src/lib" para exigir dónde vive la lógica de esta puerta, a diferencia de lo que sí hace, por ejemplo, `features/pagina_blog.feature @s18`/`@s29` (citado por L3). `stryker.config.json` confirma `"mutate": ["src/lib/**/*.ts", "src/**/*-logica.ts", ...]`: si el inventario, los umbrales y el veredicto de esta puerta viven en el `.tsx` o en el propio `.test.ts`, StrykerJS no genera mutantes para ellos y la "puerta transversal de cierre del proyecto" (L3 de la Feature) cierra con mutación al 100 % sobre una superficie vacía. |
| @s20 (y @s21) | grave | **CONFIRMADO** | L3 | Verificado línea a línea: `@s19` (L309) lleva "And el recuento de elementos comprobados es mayor que 0" y `@s22` (L332) lleva "And el recuento de elementos con animación en curso es mayor que 0"; `@s20` (L312-317) y `@s21` (L319-325) cuantifican sobre "todo el contenido que en condiciones normales aparece mediante una animación" sin ningún recuento. Con el conjunto de elementos animados vacío, ambos son verdes vacíos justo en los dos escenarios que protegen el Invariante 4 (`project-spec.md` L58-60) del estado base visible. |
| @s27 | grave | **CONFIRMADO** | L3 | El Then "el foco solo se detiene en los controles de cabecera" (L377) es un universal sobre el mismo recorrido cuyo único recuento explícito es "el recuento de paradas dentro de los paneles colapsados es exactamente 0" (L379) — un cero que también sería cierto si el tabulador no se moviera en absoluto. A diferencia de `@s23` (L346, "el número de paradas del recorrido es mayor que 0" sobre la página entera), `@s27` no tiene ningún recuento positivo propio que demuestre que el recorrido del tabulador llegó siquiera a los controles de cabecera del acordeón. |
| @s13 (y @s14) | grave | **CONFIRMADO** | L3 | La cabecera transcribe cinco excepciones literales de SC 2.5.8: "(a) Spacing...; (b) Equivalent...; (c) Inline...; (d) User Agent Control; (e) Essential" (L23-29). De los escenarios del bloque B, `@s13` contrata solo la excepción "en línea" (Inline, L247) y `@s14` solo la de "espaciado" (Spacing, L256); ningún escenario del fichero contrata Equivalent, User Agent Control ni Essential. Si el módulo declara las cinco (lo que la propia cabecera induce a hacer, al citarlas todas como norma), tres quedan sin ejercitar, contra `thresholds.break: 100`. |
| @s33 | menor | **CONFIRMADO** | L3 | El Given "un inventario de parejas de color sin ninguna entrada" (L429) no dice a cuál de los tres inventarios de color del bloque F (texto normal `@s29`, texto grande `@s30`, componentes de interfaz `@s31`) se aplica. Es la mitad reforzante del hallazgo ya confirmado sobre `@s30`: un implementador puede satisfacer la regla "una guarda por extractor" (cabecera L79-80) con una sola guarda genérica y dar la regla por cumplida, dejando los otros dos inventarios sin su propio escenario de fallo cerrado. |

## Resumen

- Total alegado: 17
- Confirmado: 13 (5 bloqueantes de L1, 2 bloqueantes de L3 sobre @s18/@s30 con guarda de conteo — nota: @s18-guarda es grave, no bloqueante, ver tabla — más @s32 y CABECERA bloqueantes de L3, @s20/@s21, @s27, @s13/@s14 graves de L3, @s10 grave de L1, @s33 menor de L3)
- Refutado: 4 (@s16 y @s24 y @s29 de L1, @s30-umbral-genérico de L2)
- Duplicados colapsados: 0
