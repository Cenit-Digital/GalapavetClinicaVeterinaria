# Veredicto — features/selector_paleta.feature (grupo G1)

Verificador independiente. Alegaciones recogidas de L1_G1.md (1), L2_G1.md (3) y
L3_G1.md (0 — el informe L3 declara cobertura 16/16 escenarios + cabecera para
este fichero pero no reporta ningún hallazgo sobre él). Total alegado: **4**.
Duplicados colapsados entre lentes: **0** (cada alegación tiene un ancla y un
defecto de fondo distintos; ninguna lente repite a otra sobre este fichero).

| Ancla | Severidad | Veredicto | Lente(s) | Motivo (cita propia) |
| --- | --- | --- | --- | --- |
| @s10 (línea 141, `Given el documento HTML generado por la construcción del sitio`) | menor | **REFUTADO** | L1 | `package.json` fija `"test": "vitest run"` y `harness.config.json` invoca `pnpm run test` — no hay `vite build` en el camino de test, tal y como observa el propio L1. Pero eso no hace inviable el escenario: en el `index.html` fuente del repo ya existe `<script type="module" src="/src/main.tsx">` (línea 11 de `index.html`), que es exactamente "un script del paquete de la aplicación" contra el que @s10 pide comparar orden y ausencia de `defer`/`async`/`src`. Los tres Then de @s10 (orden, ausencia de `defer`/`async`, ausencia de `src`/petición de red) son propiedades estáticas del marcado que se verifican igual de bien leyendo el `index.html` versionado que un artefacto de `dist/`, sin necesidad de invocar una build real. Existe pues una lectura razonable y de bajo coste bajo la cual el contrato es satisfacible con el arnés tal cual está declarado — la misma que el propio L1 apunta como resolución probable ("Riesgo bajo... probablemente se resuelve contra el index.html fuente"), lo que socava la propia alegación de defecto. |
| CABECERA, fila `lima` / @s2 (línea 21 `\| lima \| Lima protagonista \| Lima sobre claro \|`; línea 85) | grave | **CONFIRMADO** | L2 | Verifiqué la cita: línea 21 y línea 85 dicen exactamente lo que L2 transcribe, y `docs/datos-galapavet.md` líneas 160-168 fijan, con la fórmula oficial, que `#B4C718` sobre blanco da 1,89:1 y que "Cualquier uso del lima como color de texto o de borde sobre fondo claro es un defecto de accesibilidad, no una preferencia estética." La nota "Lima protagonista / Lima sobre claro" no distingue el uso legítimo (lima como superficie con texto oscuro encima, 11,12:1 según la propia tabla) del uso prohibido (lima como texto/borde protagonista), y "protagonista" connota precisamente el segundo. A diferencia del caso `noche` (ver fila siguiente), aquí sí existe una puerta que atajaría una implementación equivocada — `features/tokens_marca.feature` @s6 ("El verde lima sobre blanco no es apto para ningún uso de texto ni de interfaz", líneas 121-127) — pero eso solo cambia la consecuencia (rehacer trabajo cuando @s16 rechace el catálogo) sin resolver la ambigüedad de la nota, que es el defecto que se alega. La reparación propuesta por L2 (fijar el uso, no el protagonismo) es correcta. |
| CABECERA, fila `noche` (línea 23 `\| noche \| Marca en oscuro \| Morado y lima sobre oscuro \|`; líneas 55-57; `tokens_marca.feature` líneas 64-66) | grave | **CONFIRMADO** | L2 | Verifiqué las cuatro citas de L2: `selector_paleta.feature` línea 23 y líneas 55-57 (el PENDIENTE que remite el token del fondo oscuro a `tokens_marca`) existen tal cual; `tokens_marca.feature` línea 64 dice literalmente "la variante oscura de la Decisión 8 se contrata en la feature `selector_paleta`" — cada fichero remite al otro y ninguno fija el hexadecimal, es una referencia circular real, no una lectura torcida de L2. Recalculé la aritmética de forma independiente con la fórmula oficial de luminancia relativa sobre `#77286B` (R=119,G=40,B=107 → R_lin≈0,1845, G_lin≈0,0212, B_lin≈0,1471 → L=0,2126·0,1845+0,7152·0,0212+0,0722·0,1471≈0,0650) y confirmo el ratio máximo de L2 contra negro puro: (0,0650+0,05)/(0+0,05)=2,30:1. Fui más allá y despejé qué luminancia de fondo permitiría que el morado alcanzara 3:1 como color legible sobre un fondo "oscuro" más oscuro que él: (0,065+0,05)/(L_fondo+0,05)≥3 exige L_fondo≤-0,0117, negativo e imposible para cualquier color real. Es decir: no hay ningún fondo suficientemente oscuro (siempre que sea más oscuro que el propio morado) contra el que el morado alcance el mínimo de 3:1 de componente de interfaz — la nota "Morado y lima sobre oscuro" contrata una combinación que la propia aritmética de la fuente hace irrealizable si morado actúa como elemento legible sobre el fondo oscuro, y el círculo de referencias entre ambos ficheros deja that sin resolver ni decidir. |
| CABECERA general (líneas 2-9, 15-17) | menor | **CONFIRMADO** | L2 | Verifiqué exhaustivamente: no existe ningún `.dc.html` en todo el repositorio (`find . -iname "*.dc.html"` sin resultados); el zip `docs/contrato-heredado/veterinarialasierra-gherkin.zip` archiva 13 `.feature` + un README, ninguno un `.dc.html`; `docs/contrato-heredado/selector_paleta.feature` (el contrato heredado archivado, que sí pude leer completo) no contiene la cadena "Cambiar paleta de color" en ningún punto — solo "un botón circular flotante en la esquina inferior derecha para cambiar de paleta" (su línea 23) y un panel titulado "Paleta de color" (no "Cambiar paleta de color"); tampoco contiene los descriptores "azul cobalto", "cian neón" ni "esmeralda" que las líneas 15-17 del fichero bajo revisión dan por heredados (`grep -rln` sin resultados en `docs/contrato-heredado/`). La cabecera afirma "La INTERACCIÓN heredada está aprobada y se respeta" citando literales que ningún artefacto del repositorio sostiene, incumpliendo la misma norma de trazabilidad que este contrato exige para los datos de negocio. Corrijo un detalle de la propia alegación de L2: cita "seis escenarios" pero solo lista cinco tags (@s1, @s2, @s3, @s4, @s16); comprobé el fichero completo y en efecto son 5, no 6, los escenarios que citan el nombre accesible "Cambiar paleta de color" — un error menor de recuento en L2 que no invalida el fondo del hallazgo. |

## Resumen

- Total alegado: 4
- Confirmados: 3
- Refutados: 1
- Duplicados colapsados: 0
