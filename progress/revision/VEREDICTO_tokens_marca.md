# VEREDICTO — features/tokens_marca.feature (G1)

Verificador independiente. 7 alegaciones recogidas de L1_G1.md, L2_G1.md y L3_G1.md
sobre este fichero. 0 se colapsan por duplicidad (cada una ataca un defecto de
fondo distinto, incluso las que comparten ancla `@s16`). 4 confirmadas, 3 refutadas.

## Tabla de veredictos

| Ancla | Severidad | Veredicto | Lentes | Motivo (cita propia) |
| --- | --- | --- | --- | --- |
| @s18 | grave | **CONFIRMADO** | L1 | El Given de @s18 es "todos los ficheros SCSS del proyecto salvo el fichero de tokens de marca" (línea 224), sin fixture. `tokens_marca` es la feature `id: 1` de `feature_list.json` (líneas 22-23), la primera del pipeline, y `src/` en el repo solo contiene `.gitkeep` — cero ficheros SCSS de ningún tipo existen todavía. Cuando el `tdd_craftsman` implemente y verifique este escenario de forma autónoma (como exige `sdd:true`), el conjunto escaneado será vacío por construcción, y el Then "el número de ficheros inspeccionados... es mayor que 0" (línea 227) fallará por una razón ajena a la lógica de la puerta. |
| CABECERA / @s8 / @s16 | grave | **CONFIRMADO** | L2 | La cabecera dice, en presente de pendiente: "PENDIENTE: la pareja #48704B sobre #B4C718 alcanza 3.01... Confirmar con diseño si se quiere usar o si se prefiere holgura" (líneas 61-63). Pero `docs/datos-galapavet.md:175` ya cierra esa pregunta con un veredicto sin condicional: "El par `verde sobre lima` pasa el 3:1 por una centésima. **No se usa**". El contrato reabre como abierta una decisión que la fuente única ya tomó, y @s16 (líneas 200-208) no añade ninguna cláusula que excluya esa pareja del catálogo declarado — la puerta de contraste dejaría pasar exactamente el par que el diseño descartó. Nota: @s8 en sí mismo no es incorrecto (mide la función de aptitud matemática, no una decisión de uso), el defecto está en la cabecera y en la ausencia de guarda en @s16. |
| @s8 | grave | **CONFIRMADO** | L3 | @s9 fija el límite exacto de "texto normal" en 4.5 (línea 147) y @s10 lo hace un céntimo por debajo en 4.49 (línea 153); @s11 fija el límite exacto de "componente de interfaz o borde de foco" en 3.0 (línea 159). Pero "texto grande" comparte el mismo umbral 3:1 (cabecera línea 46) y jamás se prueba exactamente en el límite: @s8 (líneas 136-143) solo usa 3.01. Si "texto grande" tiene su propia rama de comparación (`>=3` con lógica separada de "componente"), un mutante `ConditionalExpression` que cambie `>=` por `>` únicamente en esa rama no lo mata ningún Then de este fichero. |
| @s16 | grave | **CONFIRMADO** | L3 | Los Then de @s16 (líneas 205-207) exigen "ninguna pareja de uso «texto normal/grande/componente» queda por debajo de" su mínimo, pero la única guarda de no-vacuidad es "el número de parejas evaluadas... es mayor que 0" (línea 208), un conteo GLOBAL. Si la clasificación por uso se rompe (p. ej. todo cae en "componente" y las otras dos categorías quedan vacías), las cláusulas de "ninguna pareja de uso X" se cumplen por vacuidad y el conteo total sigue siendo mayor que 0: la puerta pasa verde sin haber comprobado nada de dos de sus tres categorías. |
| @s16 | menor | REFUTADO | L1 | La propia feature usa el patrón "`el <artefacto> de marca del proyecto`" para referirse a un artefacto concreto y autocontenido, no a un escaneo: @s1 abre con "Given el fichero de tokens de marca **del proyecto**" (línea 80) refiriéndose sin ambigüedad al módulo de tokens que la propia feature define. @s16 sigue el mismo patrón: "el catálogo de parejas color/fondo **del proyecto**" (línea 202) se lee, por precedente interno del propio fichero, como el catálogo que ese mismo módulo declara — no como un escaneo de todos los usos reales del repo. La lectura ambigua que alega L1 existe en abstracto, pero no es la lectura que el propio fichero establece por su uso reiterado de "del proyecto" para nombrar artefactos propios. Distinto es @s18, que dice explícitamente "todos los ficheros... del proyecto", un cuantificador universal sobre el sistema de ficheros que @s16 no usa. |
| @s1 | menor | REFUTADO | L2 | @s1 exige "el número total de colores de marca declarados es exactamente 3" (línea 85) y la propia cabecera ya anuncia que la fuente ofrece "LOS TRES HEXADECIMALES" (línea 9) — es una elección deliberada, no un descuido: `project-spec.md:89` (Decisión 8) define la identidad de marca como "morado `#77286B` + lima `#B4C718`" (ni siquiera incluye el verde profundo), así que la propia feature ya amplía el criterio del proyecto al incluir el verde profundo entre los "colores de marca" y excluye conscientemente el blanco, que en `docs/datos-galapavet.md:139` figura con el rol "fondo del disco" — un color base/neutro, no un color distintivo de la marca. @s20 (línea 239) no obliga a contradecir esto: esa puerta prohíbe literales de color CSS fuera del fichero de tokens para *cualquier* color, no solo para los "colores de marca"; nada impide que el mismo fichero de tokens declare el blanco como token base sin contarlo entre los 3 "colores de marca". |
| CABECERA | menor | REFUTADO | L2 | Es cierto que la tabla de ratios de la cabecera (líneas 39-41) no incluye el par "negro sobre lima → 11.12" que `docs/datos-galapavet.md:155` documenta como "el uso correcto" del lima. Pero la cabecera declara explícitamente el alcance de la feature: "Este contrato cubre color y contraste" para servir al cálculo y a la clasificación de aptitud (líneas 32-48), no una guía exhaustiva de combinaciones válidas de producción — y cuando la feature sí deja fuera algo que la fuente considera relevante, lo declara como PENDIENTE explícito (cuatro veces, líneas 50-66); esta ausencia no está marcada como tal, lo que sugiere omisión menor, no contradicción. Además la pareja 11.12 no añade ningún límite nuevo a la función de aptitud: excede ampliamente los tres umbrales (4.5/3/3) igual que @s3 (9.13) ya excede el de texto normal, así que su ausencia no debilita la cobertura de mutación de los límites, que es donde @s9-@s11 ya concentran la prueba de frontera. No hay ninguna afirmación falsa en el fichero achacable a esta ausencia — es un hueco de documentación, no una infidelidad a la fuente. |

## Resumen

- Total alegado: 7
- Confirmados: 4 (@s18 grave, CABECERA/@s8/@s16 grave, @s8 grave, @s16 grave)
- Refutados: 3 (@s16 menor, @s1 menor, CABECERA menor)
- Duplicados colapsados: 0
