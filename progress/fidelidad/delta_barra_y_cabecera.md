# Delta de fidelidad — Barra de urgencias fija y cabecera

> Sección: barra superior de urgencias + `<header>` (logotipo con marca gráfica,
> navegación, botón rojo de urgencias, botón Tienda, hamburguesa y panel móvil).
> Prototipo: `docs/diseno-claude-design/Veterinaria La Sierra.dc.html` (citado
> como `VLS:<línea>`). Web: `src/components/BarraUrgencias.*`,
> `src/components/Cabecera.*`, `src/data/navegacion.ts`, `src/lib/site.ts`.
> Capturas a 1280 px: `scratchpad/shots/diseno_00.png` (prototipo) y
> `scratchpad/shots/web_00.png` (web), franja superior de cada una.
>
> Regla de lectura: cada valor del prototipo se cita con su línea; cada valor
> de la web, con su fichero y línea. Cuando un número del prototipo no cae en la
> escala del repo (`src/styles/_api.scss`), manda la escala y la desviación se
> declara (cabecera de `features/rediseno_visual.feature`, "ADVERTENCIA sobre la
> Decisión 24").

## Anatomía del prototipo

### Correspondencia de tokens (prototipo → sistema del repo)

| Prototipo (`VLS:18-24`) | Repo (`src/styles/_tokens.scss`) |
| --- | --- |
| `--bg` | `--color-fondo` |
| `--bg-2` | `--color-fondo-alterno` |
| `--card` | `--color-superficie` |
| `--surface` | `--color-superficie-elevada` |
| `--border` | `--color-borde` (decorativo) / `--color-borde-control` (identifica un control, SC 1.4.11) |
| `--ink` | `--color-tinta` |
| `--text` | `--color-texto` |
| `--muted` | `--color-texto-suave` |
| `--primary` / `--primary-strong` | `--color-primario` / `--color-primario-fuerte` |
| `--on-primary` y el `#fff` literal sobre `--urg` | `--color-sobre-primario` (@s7: nunca blanco literal sobre urgencia) |
| `--accent` / `--accent-ink` / `--accent-soft` | `--color-acento` / `--color-acento-tinta` / `--color-acento-suave` |
| `--urg` / `--urg-soft` | `--color-urgencia` / `--color-urgencia-suave` |
| `--shadow` / `--shadow-sm` | `--sombra-elevada` / `--sombra-reposo` |

### A. Barra de urgencias (`VLS:66-75`)

- Envoltorio: `<sc-if value="{{ barraUrgencias }}">` (prop `mostrarBarraUrgencias`, por defecto `true`, `VLS:692`) que envuelve un `<div>` **sin landmark**. Estilos (`VLS:67`): `background: var(--urg)`; `color: #fff`; `font-size: 13.5px`; `display: flex`; `flex-wrap: wrap`; `align-items: center`; `justify-content: center`; `gap: 6px 14px`; `padding: 9px 18px`; `text-align: center`.
- **No es fija ni sticky**: se desplaza con la página. Solo el `<header>` es `sticky` (`VLS:77`), así que al hacer scroll la barra desaparece y la cabecera se pega a `top: 0`.
- Altura computada: 13,5 px × interlineado normal (~1,4) + 18 px de padding ≈ **37 px**. En `diseno_00.png` ocupa `y = 0…34`.
- Tres hijos, centrados en una fila:
  1. `<span>` (`VLS:68`) `display: inline-flex; align-items: center; gap: 8px; font-weight: 700`, que contiene el **punto pulsante** `<span>` (`VLS:69`) `width: 8px; height: 8px; border-radius: 50%; background: #fff; animation: vlsPulso 1.6s ease-in-out infinite` y el texto "Urgencias 24 h" (**falso para Galapavet**, no se porta el texto).
  2. `<span style="opacity: .9">` (`VLS:72`): frase secundaria en peso normal, "Atención inmediata todos los días del año ·" (**falsa**, no se porta).
  3. `<a href="tel:+34640221190">` (`VLS:73`) `color: #fff; text-decoration: underline; font-weight: 700` con el teléfono `{{ telUrgencias }}`. **Solo el teléfono es enlace**; el rótulo no.
- `@keyframes vlsPulso` (`VLS:58`): `0%,100% { opacity: 1; transform: scale(1) }` · `50% { opacity: .35; transform: scale(.78) }`. Con `prefers-reduced-motion: reduce` (`VLS:59`) todas las animaciones pasan a `.01ms`.

### B. Cabecera (`VLS:77-116`)

- `<header>` (`VLS:77`): `position: sticky; top: 0; z-index: 60; background: color-mix(in srgb, var(--bg) 88%, transparent); backdrop-filter: blur(14px); -webkit-backdrop-filter: blur(14px); border-bottom: 1px solid var(--border)`. Sin altura declarada: la fija el contenido → **63 px en escritorio** (38 de marca + 12 + 12 de padding + 1 de borde) y **71 px en móvil** (46 de hamburguesa + 24 + 1) (`progress/rediseno/matriz_delta.md` C-13). En `diseno_00.png`: `y = 34…98`.
- Fila interior `<div>` (`VLS:78`): `max-width: 1220px; margin: 0 auto; padding: 12px clamp(16px, 4vw, 28px); display: flex; align-items: center; justify-content: space-between; gap: 16px`. A 1280 px: caja `x = 30…1250`, contenido `x = 58…1222` (padding = 28 px porque 4vw = 51 px).
- **Logotipo** `<a href="#inicio">` (`VLS:79`): `display: flex; align-items: center; gap: 11px; color: var(--ink); flex-shrink: 0`. Contiene, **los dos dentro del enlace**:
  - Marca gráfica `<span>` (`VLS:80`): `position: relative; width: 38px; height: 38px; border-radius: 12px; background: var(--primary); display: flex; align-items: center; justify-content: center; flex-shrink: 0`, con dos `<span>` absolutos (`VLS:81-82`) de `17px × 4.5px` y `4.5px × 17px`, `border-radius: 3px; background: var(--on-primary)`: una **cruz genérica dibujada en CSS**, no un logotipo real. En la captura: cuadrado azul `x = 58…96`.
  - Wordmark `<span>` (`VLS:84`) `display: flex; flex-direction: column; line-height: 1.05`: nombre (`VLS:85`) `font-family: 'Outfit'; font-weight: 600; font-size: 17px; letter-spacing: -.01em`; descriptor (`VLS:86`) `font-size: 10.5px; letter-spacing: .16em; text-transform: uppercase; color: var(--muted); font-weight: 600`. En la captura el wordmark termina en `x ≈ 262`.
- **Navegación** `<nav aria-label="Navegación principal">` (`VLS:90`): `display: flex` (o `none` si `ancho < 1120`, decidido **en JS**, `VLS:684` y `:695`; el prototipo no tiene ningún `@media`); `align-items: center; gap: 2px`. Contenido, en este orden:
  - 7 enlaces (`VLS:92`, datos en `VLS:702-710`: Reservar `#reservar`, Servicios `#servicios`, Campañas, Equipo `#equipo`, Blog, Contacto `#contacto`, FAQ `#faq`): `padding: 9px 11px; border-radius: 999px; font-size: 14px; font-weight: 500; color: var(--text); white-space: nowrap; transition: background .3s ease, color .3s ease`; hover `background: var(--accent-soft); color: var(--accent-ink)`. Altura ≈ 38 px. Sin estado "página actual".
  - **Píldora roja de urgencias** `<a href="#contacto">` (`VLS:94-96`): `display: inline-flex; align-items: center; gap: 7px; margin-left: 6px; padding: 9px 15px; border-radius: 999px; background: var(--urg); color: #fff; font-size: 13.5px; font-weight: 700; white-space: nowrap; transition: filter .3s ease`; hover `filter: brightness(1.08)`. Dentro, punto `7px × 7px; border-radius: 50%; background: #fff; animation: vlsPulso …` (`VLS:95`) y el texto "Urgencias". En la captura: `x ≈ 1029…1138` (109 px de ancho), `y ≈ 50…83`. **Va antes de Tienda.**
  - **Tienda** `<a href="./Tienda.dc.html">` (`VLS:97`): `margin-left: 4px; padding: 8px 15px; border-radius: 999px; border: 1.5px solid var(--border); color: var(--ink); font-size: 13.5px; font-weight: 600; white-space: nowrap; transition: border-color .3s ease`; hover `border-color: var(--primary)`. En la captura: `x ≈ 1145…1222` (77 px).
- **Hamburguesa** `<button type="button" aria-label="Abrir menú" aria-expanded="…">` (`VLS:100-104`; estilos `VLS:696-698`): `display: flex` solo si `ancho < 1120`; `width: 46px; height: 46px; flex-direction: column; align-items: center; justify-content: center; gap: 5px; border-radius: 12px; border: 1px solid var(--border); background: var(--card); cursor: pointer`. Tres `<span>` `width: 19px; height: 2px; border-radius: 2px; background: var(--ink); display: block`. **Sin `aria-controls`**; `aria-label` fijo aunque esté abierto. No lleva texto visible.
- **Panel móvil** (`VLS:107-115`): `<div>` renderizado solo con `menuAbierto`, **dentro del `<header>` y en flujo** (empuja la altura del sticky): `border-top: 1px solid var(--border); background: var(--bg); padding: 14px clamp(16px, 4vw, 28px) 22px; display: flex; flex-direction: column; gap: 4px; max-height: 70vh; overflow-y: auto`.
  - 7 enlaces + Tienda con el **mismo estilo** (`VLS:110`, `:112`): `padding: 13px 12px; border-radius: 12px; font-size: 16px; font-weight: 500; color: var(--ink); min-height: 46px; display: flex; align-items: center`; hover `background: var(--accent-soft)`; `onClick = cerrarMenu`.
  - Cierre: **CTA rojo** `<a href="tel:+34640221190">` (`VLS:113`): `margin-top: 8px; padding: 14px; border-radius: 999px; background: var(--urg); color: #fff; font-weight: 700; text-align: center; min-height: 48px; display: flex; align-items: center; justify-content: center`; texto "Urgencias 24 h · {{ telUrgencias }}". No cierra el menú.
- Otros valores que afectan a la sección: `html { scroll-padding-top: 88px }` (`VLS:51`, no coincide ni con 63 ni con 71); punto de corte 1120 en JS (`VLS:684`); fuentes Outfit/DM Sans desde Google Fonts (`VLS:15`; en el repo son autoalojadas, `global.scss` sección E).

## Estado actual de la web

### A. `BarraUrgencias`

- DOM (`src/components/BarraUrgencias.tsx:9-12`): `<aside class="barra" aria-label="Urgencias fuera de horario">` → `<span aria-hidden="true">●</span>` + **un único** `<a href="tel:+34918511393">Urgencias fuera de horario · 91 851 13 93</a>`. Rótulo, separador y teléfono son **una sola cadena de texto** dentro del enlace.
- Estilos (`BarraUrgencias.module.scss`): `.barra` `position: fixed; inset-block-start: 0; inset-inline: 0; z-index: 30` (`:2-5`); flex centrado, `gap: espaciado(8)` (`:9`); `min-height: var(--altura-barra-urgencias)` = 32 px (`:10`, `global.scss:50`); `padding-inline: espaciado(16)` (`:11`); `background-color: var(--color-urgencia); color: var(--color-sobre-primario)` (`:12-13`); `font-size: paso-tipografico(-1)` = 12,8 px, `font-weight: 700`, centrado (`:14-16`). El `span` lleva `animation: pulso 1.6s ease-in-out infinite` dentro de `no-preference` (`:30-33`) y `@keyframes pulso { 50% { opacity: .45 } }` (`:53-56`). El `a` es `inline-flex`, `min-height: 24px`, `color: inherit`, `text-decoration: none`, foco con `outline-color: var(--color-sobre-primario)` (`:37-49`).
- Captura `web_00.png`: franja roja `y = 0…32`; texto "● Urgencias fuera de horario · 91 851 13 93" **todo en negrita, del mismo tamaño y sin subrayado**: el teléfono no se distingue del rótulo y nada indica que sea pulsable. El punto es el glifo `●` de la fuente (tamaño del texto, no un círculo de 8 px). No hay frase secundaria. La barra es 2-5 px más baja y su texto 0,7 px más pequeño que en el prototipo. **A diferencia del prototipo, es fija** (se mantiene al hacer scroll): eso es lo que contrata `rediseno_visual.feature` @s27 ("barra de urgencias fija") y se respeta.

### B. `Cabecera`

- DOM (`src/components/Cabecera.tsx:114-149`): `<header class="cabecera">` → `<div>` [ `<a href="#inicio">Galapavet</a>` + `<p>Centro integral veterinario</p>` (**el descriptor está fuera del enlace**) ] → (escritorio) `<nav aria-label="Navegación principal">` → `<ul>` → 8 `<li><a>` (Tienda es el 8.º, con `data-enlace-tienda`; el enlace de la subpágina activa lleva `aria-current="page"`) | (móvil) `<button aria-expanded aria-controls>Abrir menú</button>` (texto visible) + `<div id class="panelMovil">` → `<ul>` con los mismos 8. **No existe ninguna marca gráfica, ninguna `<img>`, ningún control de urgencias ni ningún enlace `tel:`** en toda la cabecera (`Cabecera.test.tsx` @s13 lo vigila).
- Estilos `.cabecera` (`Cabecera.module.scss:11-26`): `position: fixed; min-height: var(--altura-cabecera)` = 64 px; `inset-block-start: var(--altura-barra-urgencias)` = 32 px; `inset-inline: 0; z-index: 10; display: flex; align-items: center; justify-content: space-between; gap: espaciado(16); padding-block: espaciado(12); padding-inline: espaciado(24); background-color: var(--color-fondo)` (**opaco**); `color: var(--color-texto); border-block-end: 1px solid var(--color-borde); font-size: paso-tipografico(0)` = 16 px. **No hay contenedor de 1220 px**: el contenido se pega a 24 px de cada borde de la ventana.
- Marca (`:31-52`): columna `line-height: 1.2`; `a` Outfit **700** / `paso-tipografico(1)` = 20 px / `--color-tinta`; `p` `paso-tipografico(-2)` = 10,24 px / 600 / `letter-spacing: .14em` / uppercase / `--color-texto-suave`.
- `.navPrincipal` (`:56-112`): `display: none` + `@media (min-width: 1024px) { display: block }`; `ul` flex `gap: espaciado(8)`; `a` `inline-flex; min-height: 24px` (`area-tactil-minima`); `padding-inline: espaciado(12)`; radio píldora; `font-weight: 600`; 16 px heredados; hover `--color-acento-suave` / `--color-acento-tinta` (coincide con el prototipo); `[aria-current='page'] { color: var(--color-primario) }`. Tienda: `@include boton-fantasma` (`:110-112`) → **48 px de alto**, `padding-inline: 20px`, borde 1,5 px `--color-borde-control` (gris oscuro), 600, 16 px.
- `.botonMenu` (`:115-122`): `boton-fantasma` (píldora de 48 px con el texto "Abrir menú" visible), oculto desde 1024.
- `.panelMovil` (`:125-160`): `position: fixed; inset-block-start: calc(64px + 32px)` = 96 px; `z-index: 9; max-height: 70vh; overflow-y: auto; background-color: var(--color-fondo); border-block-end` + `box-shadow: var(--sombra-elevada)`; `ul` columna, `gap: espaciado(4)`, `padding: espaciado(16)`; `a` `flex; min-height: 24px; padding-inline: espaciado(12); border-radius: 12px; font-weight: 600`; Tienda `boton-fantasma`.
- Captura `web_00.png`: cabecera `y = 32…~104`; "Galapavet" en Outfit negrita 20 px con el descriptor debajo, pegado a `x = 26`; **sin marca gráfica**; navegación de 8 ítems en 16 px semibold repartida hasta `x ≈ 1256`, con "Tienda" como píldora de borde oscuro **48 px de alto** (más grande que cualquier ítem del prototipo, que mide 38); **no hay píldora roja de urgencias**. El contenido de la cabecera (`x = 26…1256`) no alinea con el de las secciones (contenedor de 1220 px: `x = 54…1226`).
- **Defecto medible (bug)**: la píldora "Tienda" mide 48 px (`$altura-control-media`) y el `padding-block` suma 24 px → la cabecera **mide ~73 px reales**, no los 64 px de `--altura-cabecera` que `#root` (`global.scss:151`), `scroll-padding-top` (`:296`), `.salto-contenido` (`:249`) y `.panelMovil` (`:128`) dan por buenos. Consecuencias: los 9 px superiores del contenido quedan bajo la cabecera, el panel móvil arranca 9 px por encima del borde inferior real de la cabecera (y como su `z-index` es 9 < 10, ese trozo queda tapado), y el salto a ancla reserva 9 px de menos. En móvil ocurre lo mismo con el botón "Abrir menú" (también 48 px).

## Diferencias

| id | Qué cambia | Tipo | Prioridad |
| --- | --- | --- | --- |
| barra_y_cabecera-1 | **Marca gráfica ausente en el logotipo.** Prototipo: cuadrado 38×38, radio 12, relleno primario con cruz. Web: solo texto. Se añade el logotipo real (`/img/logo-galapavet.webp`, 201×201) a 40×40 (`$altura-control-pequena`) con radio `$radio-medio`, `alt=""` (nombre por el texto contiguo, mismo patrón que `PieDePagina` @s1), dentro del enlace `#inicio`. Desviación declarada: 40 en vez de 38 (escala). | estructura | alta |
| barra_y_cabecera-2 | **Cabecera sin contenedor de 1220 px centrado.** Prototipo: fila interior `max-width: 1220px; margin: 0 auto`. Web: contenido pegado a 24 px del borde de la ventana, desalineado con las secciones. Se resuelve en el propio `<header>` con `padding-inline: max(espaciado(24), calc((100% - $ancho-maximo-contenedor) / 2 + espaciado(24)))`, sin `div` envolvente (para que `tests/e2e/accesibilidad.spec.ts:376` siga viendo los hijos directos) y **sin** `data-contenedor-principal` (`geometria-escalas.spec.ts:151` y `layout.spec.ts:49` miden ese atributo). | estructura | alta |
| barra_y_cabecera-3 | **Píldora roja de urgencias ausente en la navegación de escritorio.** Prototipo: `● Urgencias` (relleno `--urg`, 13,5 px/700, radio píldora, punto pulsante), antes de Tienda. Web: nada. Se añade como **hermana del `<nav>`** (no dentro: @s4 exige exactamente 8 enlaces en la región), relleno `--color-urgencia`, tinta `--color-sobre-primario`, `min-height: $altura-control-pequena`, `padding-inline: espaciado(16)`, `paso-tipografico(-1)`/700, punto de `espaciado(8)` en `currentColor` pulsando dentro de `no-preference`. Texto y destino: ver "Datos reales necesarios" (variante A: teléfono visible + rótulo solo para lectores, `href` `tel:`). **Requiere enmienda** (ver conflictos). | estructura | alta |
| barra_y_cabecera-4 | **Rótulo y teléfono de la barra como una sola cadena.** Prototipo: rótulo en negrita + frase secundaria + **teléfono subrayado como pieza propia**. Web: todo dentro de un enlace, mismo peso, sin subrayado. Se separa en `<strong>{rótulo}</strong>`, separador `·` y `<span>{teléfono}</span>` subrayado, **manteniendo los tres dentro del `<a>`** (así `BarraUrgencias.test.tsx` @s27 y `urgencias.spec.ts` no cambian). Sin frase secundaria: no existe dato real. | estructura | alta |
| barra_y_cabecera-5 | **Botón hamburguesa con texto visible.** Prototipo: cuadrado 46×46, radio 12, borde 1 px `--border`, fondo `--card`, tres barras 19×2 en `--ink`, `aria-label="Abrir menú"`. Web: píldora fantasma de 48 px con el texto "Abrir menú" a la vista. Se pasa a cuadrado `$altura-control-pequena` (40×40, para respetar los 64 px de cabecera), radio `$radio-medio`, borde `$ancho-borde-fino` `--color-borde`, fondo `--color-superficie`, tres barras (`width: 50%`, `height: math.div(espaciado(8), 4)`, `background-color: currentColor`, `gap: espaciado(4)`) y el texto "Abrir menú" **presente pero solo para lectores** (`accesibilidad-teclado.test.tsx` @s24 exige `textContent` no vacío; el contrato exige ese nombre accesible). Se conserva `aria-controls` (añadido del repo, @s7). | estructura | alta |
| barra_y_cabecera-6 | **Altura real de la cabecera (73 px) ≠ `--altura-cabecera` (64 px)** por los controles de 48 px + 24 px de padding (Tienda en escritorio, "Abrir menú" en móvil). Con -5 y -8 todos los controles de la cabecera pasan a 40 px y la cabecera vuelve a medir exactamente 64 + 1 de borde; la píldora nueva también mide 40. | bug | media |
| barra_y_cabecera-7 | **Fondo opaco en vez de translúcido con desenfoque.** Prototipo: `color-mix(in srgb, var(--bg) 88%, transparent)` + `backdrop-filter: blur(14px)`. Web: `--color-fondo` opaco. Se declara el opaco como respaldo y, dentro de `@supports (backdrop-filter: blur(1px))`, el `color-mix` al 88 % + `backdrop-filter: blur(espaciado(16))` (identidad_visual, PREGUNTA ABIERTA 2: "cada uno lleva su respaldo sólido"). `transparent` no es color literal para `puertaLiteralesColor.ts` (ya lo usa `Hero.module.scss:15-20`). | estilo | media |
| barra_y_cabecera-8 | **Densidad y peso de los enlaces de navegación.** Prototipo: `gap: 2px`, `padding: 9px 11px` (38 px de alto), 14 px/500. Web: `gap: 8px`, `padding-inline: 12px`, 24 px de alto mínimo, 16 px/600. Se pasa a `gap: espaciado(4)`, `min-height: $altura-control-pequena` (40), `padding-inline: espaciado(8)`, `paso-tipografico(-1)` (12,8 px) y `font-weight: 500`. Desviaciones declaradas: 12,8 en vez de 14 (la escala no tiene 14; 16 queda 2 px por encima y **no cabe a 1024 px con los dos botones**, ver riesgos), 4 en vez de 2, 40 en vez de 38. El `font-size` del propio `<header>` sigue en `paso-tipografico(0)` (`geometria-escalas.spec.ts:302-307` lo usa como representante del paso 0). | estilo | media |
| barra_y_cabecera-9 | **Tienda demasiado grande.** Prototipo: 38 px de alto, `padding: 8px 15px`, 13,5 px/600, borde 1,5 px `--border`. Web: `boton-fantasma` de 48 px, padding 20, 16 px. Se mantiene `@include boton-fantasma` (obligatorio por `Cabecera.test.tsx` @s28) y **después** se sobrescribe `min-height: $altura-control-pequena; padding-inline: espaciado(16); font-size: paso-tipografico(-1)`. El borde sigue siendo `--color-borde-control` (SC 1.4.11; desviación deliberada respecto al `--border` claro del prototipo). Mismo ajuste en el panel móvil, que hereda el mixin por @s28. | estilo | media |
| barra_y_cabecera-10 | **Panel móvil: enlaces de 24 px de alto y peso 600.** Prototipo: 46 px, 16 px/500, color `--ink`, `padding: 13px 12px`, panel con `padding: 14px 28px 22px` y `border-top`. Web: 24 px mínimo, 600, `padding: 16px`. Se pasa a `min-height: $altura-control-media` (48), `font-weight: 500`, `color: var(--color-tinta)`, `padding: espaciado(16) espaciado(24) espaciado(24)`, `border-block-start` además del inferior. | estilo | media |
| barra_y_cabecera-11 | **CTA rojo de urgencias ausente al final del panel móvil.** Prototipo: enlace `tel:` a ancho completo, radio píldora, `min-height: 48px`, `margin-top: 8px`, fondo `--urg`. Web: nada. Se añade **después del `<ul>`** dentro del panel con `href` = `datosNegocio.telefonoUrgencias.enlaceLlamada`, texto `{rótulo} · {teléfono}` (los dos de la fuente única; ver `Cabecera-logica.ts`), `min-height: $altura-control-media`, `margin-block-start: espaciado(8)`, `text-wrap: balance` (a 320 px cabe en dos líneas). **Requiere enmienda** de `cabecera_y_navegacion` @s7 y @s13. | estructura | media |
| barra_y_cabecera-12 | **Punto pulsante de la barra como glifo `●`.** Prototipo: círculo de 8×8 px con relleno. Web: carácter de texto. Se pasa a `<span aria-hidden>` vacío, `width/height: espaciado(8)`, `border-radius: $radio-circulo`, `background-color: currentColor` (hereda `--color-sobre-primario`: cumple @s7 sin declarar tinta propia ni blanco literal; `currentColor` no es literal para `puertaLiteralesColor.ts`). La animación sigue dentro de `no-preference`. | estilo | media |
| barra_y_cabecera-13 | **Anatomía del enlace del logotipo.** Prototipo: marca + nombre + descriptor **dentro** del `<a>`, `gap: 11px`, `line-height: 1.05`. Web: `<div>` con `<a>` (solo nombre) y `<p>` hermano. Se mueve todo dentro del `<a href="#inicio">` (flex fila, `gap: espaciado(12)`, wordmark en columna `line-height: 1.05`), conservando el `<p>` para el descriptor (`geometria-escalas.spec.ts:284-289` localiza `header p`). `cabecera_y_navegacion` @s12 lo permite (PENDIENTE 2: "depende de si el descriptor queda dentro del enlace o fuera"). | estructura | baja |
| barra_y_cabecera-14 | **Tipografía del wordmark.** Prototipo: nombre 17 px/600, `letter-spacing: -.01em`; descriptor 10,5 px/`.16em`. Web: nombre 20 px/**700**; descriptor 10,24 px/`.14em`. Se pasa el nombre a **600** con `letter-spacing: -0.01em` manteniendo `paso-tipografico(1)` (20 px: la escala no tiene 17 y `geometria-escalas.spec.ts:302-307` usa este enlace como representante del paso 1), y el descriptor a `letter-spacing: 0.16em`. Desviación declarada: 20 en vez de 17. | estilo | baja |
| barra_y_cabecera-15 | **Orden Urgencias → Tienda.** Prototipo: la píldora roja va **antes** de Tienda. Propuesta: después del `<nav>` (Tienda es el último enlace de la región), para no romper @s4/@s5. Si el humano prefiere el orden exacto, hay que enmendar @s4 ("8 enlaces de navegación + 1 control de urgencias") y sacar Tienda del `<ul>`. | estructura | baja |
| barra_y_cabecera-16 | **Hover de la píldora roja.** Prototipo: `filter: brightness(1.08)`. No se porta (Decisión 27: un filtro no es auditable y aclarar baja el contraste). Hover propuesto: `text-decoration: underline` + `box-shadow: var(--sombra-reposo)`, dentro de `no-preference` la transición. | estilo | baja |
| barra_y_cabecera-17 | **Barra fija (web) frente a barra en flujo (prototipo)** y 32 px / 12,8 px frente a ~37 px / 13,5 px. **Se respeta la web**: @s27 contrata "barra de urgencias fija", `--altura-barra-urgencias` = `espaciado(32)` la consumen `#root`, `.salto-contenido`, `.cabecera` y `.panelMovil`, y la escala no tiene 36-37 ni 13,5. Desviación declarada, sin cambio. | estilo | baja |

## Datos reales necesarios

| Campo que pide la anatomía | En el prototipo | Fuente real | Estado / alternativa honesta |
| --- | --- | --- | --- |
| Rótulo de urgencias (barra, píldora, CTA móvil) | "Urgencias 24 h" | `datosNegocio.telefonoUrgencias.rotulo` = "Urgencias fuera de horario" (`src/lib/site.ts:13`; `docs/datos-galapavet.md` §2) | **Existe.** Es el único rótulo admisible: `rediseno_visual` @s14 exige que todo texto con "urgencias" sea exactamente este, y `urgencias.spec.ts:116-118` lo comprueba con `toContain` sobre cada nodo de texto. Por eso la píldora **no puede decir solo "Urgencias"**. |
| Teléfono de urgencias y su `tel:` | "640 22 11 90" / `tel:+34640221190` | `telefonoUrgencias.textoVisible` = "91 851 13 93"; `telefonoUrgencias.enlaceLlamada` derivado con `enlaceLlamada()` (`site.ts:12`, `:23`) | **Existe.** Nunca se retipea (`puertaTelefonoHardcodeado.ts`). |
| Frase secundaria de la barra | "Atención inmediata todos los días del año ·" | Ninguna (`datos-galapavet.md` §7: la afirmación es FALSA; @s13/@s52 prohíben "24 h", "365", "todos los días del año") | **No existe → se deja el hueco.** La barra queda con dos piezas (rótulo + teléfono). No derivar nada del horario: cualquier frase sería una afirmación de disponibilidad que el cliente no publica. |
| Nombre comercial y descriptor del wordmark | "Veterinaria La Sierra" / "Clínica y hospital" | `datosNegocio.identidad.nombreComercial` = "Galapavet"; `.descriptor` = "Centro integral veterinario" (`site.ts:73-79`) | **Existen** (ya se pintan). Nota: el descriptor real tiene 27 caracteres frente a 18 del prototipo: en versalitas espaciadas ocupa ~200 px, lo que estrecha el sitio disponible para la navegación a 1024 px. |
| Marca gráfica | Cuadrado azul con cruz genérica en CSS | `public/img/logo-galapavet.webp` (201×201, 4.744 B; `docs/datos-galapavet.md` §10). `logo galapavet.webp` en la raíz del repo es un **duplicado byte a byte no servido**: no se usa. | **Existe.** Se usa el logotipo real en lugar de dibujar la cruz del prototipo (que es marca de la clínica ficticia). Ruta ya declarada en `PieDePagina.tsx:13`, así que `inventarioActivosPublicos` no cambia. Los colores del logo (morado/lima) no siguen la variante de paleta: es el logotipo real, no un adorno. |
| Ítems de navegación | 7 enlaces + Tienda | `ENLACES_NAVEGACION` (`src/data/navegacion.ts:24-33`): 8, Tienda incluido | **Existe.** Sin cambios. |
| Destino del control de urgencias de escritorio | `#contacto` | `#contacto` existe (`Landing.tsx`, sección de contacto con la tarjeta de urgencias); `tel:` existe (fuente única) | **Ambos existen.** Variante A (recomendada): `href` = `enlaceLlamada` (la acción real), texto visible = teléfono, rótulo solo para lectores → nombre accesible "Urgencias fuera de horario 91 851 13 93". Variante B (más literal, no cabe a 1024 px): texto visible = rótulo completo, `href` `#contacto` como el prototipo. Decide el humano en la enmienda. |
| Texto del CTA móvil | "Urgencias 24 h · 640 22 11 90" | `${rotulo} · ${textoVisible}` de la fuente única | **Derivable** sin inventar nada (ya es el formato de la barra actual). |
| Nombre accesible del botón de menú | "Abrir menú" (`aria-label`) | Contrato `cabecera_y_navegacion` @s3/@s6/@s7 | **Fijado por contrato**: texto oculto visualmente, no `aria-label` sin `textContent` (@s24 de accesibilidad). |
| Estado "página actual" en la navegación | No existe | `rutaActual` + `esPaginaActual` (`pagina_campanas.feature` @s1) | **Añadido del repo, se conserva.** |

## Conflictos con el contrato vigente

1. **`cabecera_y_navegacion.feature` @s13** ("el texto de la cabecera no contiene 'Urgencias'… ningún enlace de la cabecera tiene un destino que empiece por `tel:`") **contra `rediseno_visual.feature` @s28** ("la cabecera lleva… el acceso a urgencias… muestra un control de urgencias con el color de urgencia") **y @s26** ("con la barra de urgencias y **los dos botones nuevos** ya presentes" en la cabecera). La implementación anterior (`progress/rediseno/fix_s28_cabecera_tienda.md` §1) leyó "cabecera" como "franja" y dio @s28 por cubierto con `BarraUrgencias`; el prototipo y @s26 dicen otra cosa: hay un botón rojo **dentro del `<header>`**. @s13 nació de la Decisión 2 (17/08/2026) para impedir la afirmación falsa "24 h" y el teléfono ficticio, y la propia `rediseno_visual` (cabecera, "LA ENMIENDA") ya invirtió esa puerta: se vigila la afirmación, no el color ni la palabra. **Propuesta: enmendar @s13** a "el texto de la cabecera no contiene '24 h' ni '640 22 11 90'; el único enlace `tel:` de la cabecera es el de urgencias de la fuente única, con su rótulo real como nombre accesible", y dejar constancia en `progress/rediseno/enmiendas_contrato.md`. Sin esa enmienda aprobada por el humano, **no se implementan** -3 ni -11.
2. **`cabecera_y_navegacion.feature` @s7** ("el panel contiene exactamente 8 enlaces… ningún elemento del panel tiene un nombre accesible que contenga 'Urgencias'") contra el CTA rojo del panel móvil del prototipo (`VLS:113`). **Propuesta: enmendar @s7** a "8 enlaces de navegación en orden y, tras ellos, un único enlace de llamada al teléfono de urgencias de la fuente única". Misma enmienda que el punto 1.
3. **`cabecera_y_navegacion.feature` @s4/@s5** (la región "Navegación principal" contiene **exactamente 8** enlaces, en orden, Tienda el último). **Se respeta**: la píldora va fuera del `<nav>`; coste: orden visual Tienda → Urgencias (diferencia -15). Alternativa a decidir por el humano: enmendar @s4 y sacar Tienda del `<ul>` para replicar el orden del prototipo.
4. **Punto de corte único de 1024 px**: `cabecera_y_navegacion` @s1, `sistema_de_diseno_visual` @s25/@s26/@s27, `rediseno_visual` @s26 y `puntoDeCorte.test.ts:14-20` (un solo valor de `@media` en `Cabecera.module.scss`). Con la píldora de rótulo completo (~230 px) más el wordmark real (~250 px), la cabecera **no cabe a 1024 px** (estimación: 1130-1170 px necesarios). **Se respeta** el 1024 con la variante A compacta (teléfono visible, ~145 px) y la densidad de -8/-9 (estimación: ~990-1050 px). Alternativa: enmendar @s1 para subir el literal (1120 como el prototipo o 1220 como el contenedor), cosa que el propio contrato previó ("si la medida no da 1024, se corrige el literal de @s1"), pero que arrastra `Cabecera-logica.test.ts:7`, `accesibilidad.spec.ts:391-398`, `escalaTipografica.test.ts` y los `min-width: 1024px` que otros módulos copian "con el mismo literal" (`InformacionContacto.module.scss:88`). No se recomienda.
5. **`accesibilidad.feature` @s17** y `src/accesibilidad-foco.test.tsx:52-56` ("exactamente una cabecera `banner`, sin ninguna barra superior adicional"). **Se respeta**: la barra sigue siendo `<aside>`; nunca se convierte en `<header>` ni se mete dentro de él.
6. **`rediseno_visual` @s14** (rótulo exacto; todo `aria-label` con "urgencias" es igual al rótulo; todo enlace de urgencias deriva del número real) y **@s52**. **Se respeta**: ningún texto nuevo dice "Urgencias" a secas; el `<aside>` conserva `aria-label={rotulo}`; ningún `aria-label` nuevo con "urgencias" (la píldora usa texto oculto, no `aria-label`).
7. **`rediseno_visual` @s7** (sobre `--color-urgencia` solo `--color-sobre-primario`; nunca blanco literal) y **@s11** (matriz de uso reconciliada con el texto real, `matrizDeContraste.ts:499-500`). **Se respeta**: píldora y CTA declaran `background-color: var(--color-urgencia)` + `color: var(--color-sobre-primario)` (par ya en la matriz, `:362`); los puntos usan `currentColor`. Ningún par nuevo: el hamburguesa usa `--color-superficie` + `--color-tinta` (par `:355`).
8. **`rediseno_visual` @s27** (barra fija; indicador pulsante parado con `reduce`) y **`sistema_de_diseno_visual` @s33** / `movimientoRespetuoso.ts` (toda `animation`/`transition` dentro de `no-preference`). **Se respeta**: la barra sigue `fixed`; las animaciones nuevas (punto de la píldora) viven en `@media (prefers-reduced-motion: no-preference)`.
9. **`rediseno_visual` @s28** (Tienda con "borde y sin relleno" → `Cabecera.test.tsx:262-276` exige el literal `@include boton-fantasma` dentro de `a[data-enlace-tienda] {` en `.navPrincipal` y `.panelMovil`; salto a ancla desde la altura real medida). **Se respeta**: el include se mantiene y se sobrescribe después; `refCabecera.getBoundingClientRect().bottom` sigue midiendo la franja real (y con -6 vuelve a coincidir con las variables).
10. **`identidad_visual` @s14** y `hoja-global.test.ts:123-126` (la maquetación de la cabecera dimensiona su altura con la **misma** variable que `scroll-padding-top`). **Se respeta**: `min-height: var(--altura-cabecera)` se queda; -6 hace que además sea verdad.
11. **`identidad_visual`, PREGUNTA ABIERTA 2** (`backdrop-filter`/`color-mix` sin parque de navegadores fijado → respaldo sólido). **Se respeta** con `@supports`.
12. **Decisión 24 / `rediseno_visual` "ADVERTENCIA"** (escala del repo, nunca copiar 17 px, 13,5 px, 11 px, 38 px, 46 px). **Se respeta**; las desviaciones quedan declaradas en la tabla de diferencias.
13. **Decisión 27** (sin `filter: brightness()` en hover). **Se respeta** (-16).
14. **`pie_de_pagina` @s1** (logotipo decorativo, `alt=""`, nombre por el texto contiguo). **Se aplica el mismo patrón** en la cabecera para que `cabecera_y_navegacion` @s12 siga verdadero ("enlace cuyo nombre accesible contiene 'Galapavet'").
15. **`rediseno_visual` @s43** y `tests/e2e/imagenes.spec.ts:187-210` (toda imagen del documento, con `/img/` bloqueado, reserva alto > 0, respeta su relación de aspecto y se pinta con `--color-fondo-alterno`). **Se respeta**: la `<img>` del logotipo lleva `width={201} height={201}` y `@include hueco-de-imagen(1, 1)` como en `PieDePagina.module.scss:54-62`.
16. **`rediseno_visual` @s48** / `css-presupuesto.spec.ts:21` (techo de 8.000 B de CSS comprimido). Medido hoy: `dist/assets/index-CCVUwotx.css` = 60.706 B en bruto, **7.523 B gzip / 6.513 B brotli**: quedan entre 480 y 1.500 B de margen. **En tensión**: hay que medir tras el cambio; ver riesgos.

## Tests que romperán

### Unitarios (`src/**/*.test.ts(x)`)

| Test | Por qué |
| --- | --- |
| `src/components/Cabecera.test.tsx` → `@s13 la cabecera no anuncia urgencias ni contiene teléfonos` → `sin "Urgencias", "24 h" ni "640 22 11 90", y ningún enlace apunta a "tel:"` | La píldora (-3) mete el rótulo (contiene "Urgencias") y un `href` `tel:`. Se reescribe según la enmienda del conflicto 1 (afirma: sin "24 h", sin "640 22 11 90", un único `tel:` = `datosNegocio.telefonoUrgencias.enlaceLlamada`). |
| `src/components/Cabecera.test.tsx` → `@s7 abrir el menú móvil despliega los mismos ocho destinos` → `aria-expanded pasa a "true", aria-controls identifica un panel existente con los 8 enlaces en orden` | El CTA (-11) es el 9.º `link` del panel y su nombre contiene "Urgencias" (`enlaces.every(!includes('Urgencias'))` falla). Se reescribe: 8 primeros en orden + un último con `href` = `enlaceLlamada` y texto con el rótulo. |
| `src/components/Cabecera.test.tsx` → `@s4` y `@s5` | **Solo si** la píldora se coloca dentro del `<nav>` (9 enlaces en la región). El plan la deja fuera: no rompen. |
| `src/components/BarraUrgencias.test.tsx` → `@s27 muestra el rótulo y el enlace de llamada derivados de la fuente única` | **Solo si** el rótulo se saca del `<a>` (`toHaveTextContent(rotulo)` fallaría). El plan mantiene rótulo y teléfono dentro del mismo `<a>`: no rompe. |
| `src/components/Cabecera-logica.test.ts` | No rompe, pero **queda incompleto**: la función nueva `construirControlDeUrgencias` necesita sus propios tests o Stryker (100 % sobre `src/**/*-logica.ts`) deja mutantes vivos. |
| `src/lib/diseno/puntoDeCorte.test.ts` → `el único punto de corte declarado en Cabecera.module.scss es 1024` | Rompe si se añade cualquier `@media (min|max-width: N px)` con N ≠ 1024 en `Cabecera.module.scss` (p. ej. para acortar la píldora a anchos intermedios). `@supports` no es `@media`: no cuenta. |
| `src/lib/diseno/matrizDeContraste.test.ts` → reconciliación @s11 | Rompe si un `color: var(--color-X)` nuevo queda bajo un `background-color: var(--color-Y)` que la matriz no declare. El plan solo usa pares existentes (`sobre-primario/urgencia`, `tinta/fondo`, `tinta/superficie`, `texto-suave/fondo`, `acento-tinta/acento-suave`). |
| `src/lib/puertaLiteralesColor.test.ts` | Rompe con cualquier `#hex`, `rgb()` o nombre de color en los `.module.scss`. `currentColor` y `transparent` están permitidos (`puertaLiteralesColor.ts:35`). |
| `src/styles/movimiento-global.test.ts` y `src/lib/diseno/movimientoRespetuoso.test.ts` | Rompen si la animación del punto de la píldora o la transición del hover quedan fuera de `@media (prefers-reduced-motion: no-preference)`. |
| `src/styles/hoja-global.test.ts` → `@s14 … esa misma variable es la que la maquetación de la cabecera usa para su propia altura` | Rompe si `.cabecera` deja de declarar `min-height: var(--altura-cabecera)`. |
| `src/lib/diseno/rolesDescartados.test.ts` (@s13 afirmaciones prohibidas) | Rompe si algún texto nuevo dice "24 h", "365"… El plan no introduce ninguno. |
| `src/accesibilidad-teclado.test.tsx` → `@s24` (`boton.textContent` no vacío) | Rompe si el hamburguesa se queda solo con `aria-label` y tres `span` vacíos. El plan conserva el texto "Abrir menú" en un `span` oculto visualmente. |
| `src/accesibilidad-teclado.test.tsx` → `@s23` (recorrido con tabulador en orden del documento) | No rompe: construye la lista esperada del propio DOM; la píldora entra como un `a[href]` más, en orden. |
| `src/accesibilidad-foco.test.tsx` → `@s16`, `@s17` | No rompen: la píldora recibe foco como cualquier `link`; el `<aside>` no es `banner`. |
| `src/App.test.tsx` → `@s7`, `@s9`, `@s10`, `@s11` | No rompen (consultan roles `navigation`/`button`). |

### E2E (`tests/e2e/*.spec.ts`, contra `dist/`)

| Test | Por qué |
| --- | --- |
| `accesibilidad.spec.ts:401` → `ningún elemento de la cabecera se desborda ni se superpone con otro, ni a 1024px ni a 1023px` | **El riesgo principal.** A 1024 px la píldora es un hijo directo más del `<header>`; con rótulo completo (variante B) o con la densidad actual (16 px/600, gap 8) **desborda**. Solo pasa con la variante A + -8/-9. Hay que medirlo en Chromium antes de cerrar. |
| `accesibilidad.spec.ts:390` → `a 1024px la navegación horizontal es visible; a 1023px, el botón de menú` | No rompe mientras el botón siga siendo `getByRole('button', { name: 'Abrir menú' })` (texto oculto visualmente cuenta como nombre) y la región siga con `aria-label`. |
| `accesibilidad.spec.ts:271` → `@s40 … ningún control enfocado queda tapado por la cabecera fija` | No rompe (los controles del propio `<header>` se excluyen, `:317`); **mejora** con -6 porque la caja real de la cabecera vuelve a ser la reservada. |
| `accesibilidad.spec.ts` → axe, 30 combinaciones (@s45) | Rompe si la píldora/CTA pintan un par sin 4,5 (no: `sobre-primario/urgencia` da 4,83…6,04 en las cinco variantes, `BarraUrgencias.module.scss:22-24`) o si el botón de menú pierde su nombre accesible. La cabecera translúcida puede salir como `incomplete` en axe (no viola). |
| `geometria-escalas.spec.ts:284-307` → `los seis pasos inferiores … siguen siendo tamaños fijos` | Rompe si cambia el `font-size` de `header` (paso 0, 16 px), de `header a[href="#inicio"]` (paso 1, 20 px) o de `header p` (paso -2, 10,24 px). Por eso -14 mantiene 20 px en el enlace del logotipo y el descriptor sigue siendo un `<p>` (y el hamburguesa **no** debe usar `<p>`). |
| `geometria-escalas.spec.ts:143-161` → `@s17 … 1220px exactos, el mismo en las seis rutas` y `layout.spec.ts:49` | Rompen si se añade `data-contenedor-principal` al `<header>` (el `.first()` pasaría a ser la cabecera). El plan no lo añade. |
| `urgencias.spec.ts:96` → `las seis rutas rotulan las urgencias con el dato real y enlazan al teléfono real` | Rompe si algún nodo de texto contiene "urgencias" sin el rótulo completo (p. ej. una píldora que diga "Urgencias"), si un `aria-label` nuevo con "urgencias" no es exactamente el rótulo, o si un `a[href^="tel:"]` cuyo nombre (propio + `aria-label` del contenedor) mencione urgencias no apunta a `enlaceLlamada('91 851 13 93')`. El plan cumple las tres (texto oculto = rótulo completo; sin `aria-label` nuevo; `href` de la fuente única). |
| `rediseno-visual.spec.ts:61` → `aside[aria-label="Urgencias fuera de horario"] a` con `href` `tel:` | Rompe si la barra deja de ser `<aside>` con ese `aria-label` o si el teléfono deja de estar dentro de un `<a>`. El plan lo conserva. |
| `imagenes.spec.ts:25-40` y `:187-210` | Rompen si la `<img>` del logotipo no carga (ruta ya servida: no), o si con `/img/` bloqueado no reserva alto/aspecto/fondo alterno (por eso `hueco-de-imagen(1, 1)` + `width`/`height`). |
| `movimiento.spec.ts:48` → con `reduce` ninguna animación `running` | Rompe si la animación nueva del punto de la píldora no está dentro de `no-preference`. |
| `css-presupuesto.spec.ts:24` → `suma de bytes de hoja de estilo <= techo` (8.000 B) | **En tensión**: hoy 7.523 B gzip / 6.513 B brotli. Cada regla nueva cuenta; medir tras el cambio. |
| `datos-reales.spec.ts:249` → `el único compromiso de urgencias que aparece es el que declara la fuente única` | No rompe: los textos nuevos son "Urgencias fuera de horario" con el teléfono real. |

## Plan de cambio

Orden pensado para que cada paso deje verde la suite. La **fase 1** no toca ningún contrato; la **fase 2** exige la enmienda de `cabecera_y_navegacion` @s7/@s13 aprobada por el humano (conflictos 1-2). La lógica pura va en `*-logica.ts` y los `.tsx` solo cablean.

### Fase 1 — sin conflicto de contrato

1. **`src/components/BarraUrgencias.tsx` (-4, -12).** DOM: `<aside className={styles.barra} aria-label={rotulo}>` → `<a href={enlaceLlamada}>` → `<span aria-hidden="true" />` (punto vacío) + `<strong>{rotulo}</strong>` + `<span aria-hidden="true">·</span>` + `<span>{textoVisible}</span>`. Todo sigue dentro del mismo `<a>` (nombre accesible "Urgencias fuera de horario · 91 851 13 93").
   `BarraUrgencias.module.scss`: el punto pasa a `inline-block; width: espaciado(8); height: espaciado(8); border-radius: $radio-circulo; background-color: currentColor` (animación como hoy, dentro de `no-preference`); `strong { font-weight: 700 }`; el último `span` con `text-decoration: underline; text-underline-offset: espaciado(4)`; el `<a>` con `gap: espaciado(8); font-weight: 700; flex-wrap: wrap; justify-content: center`.
   **Tests**: `BarraUrgencias.test.tsx` @s27 sigue verde sin tocarlo; añadir un test que afirme que el rótulo y el teléfono son nodos distintos dentro del mismo enlace (`within(enlace).getByText(rotulo)` y `getByText(textoVisible)`) y que el punto es `aria-hidden` y vacío (no "●").
2. **`src/components/Cabecera.tsx` (-1, -13, -14) — logotipo.** Sustituir el `<div>` de marca por `<a href="#inicio" className={styles.marca}>` → `<img src={hrefDeDestino('/img/logo-galapavet.webp')} alt="" width={201} height={201} decoding="async" />` (sin `loading="lazy"`: está siempre en el viewport) + `<span>` → `<span>{nombreComercial}</span>` + `<p>{descriptor}</p>`.
   `Cabecera.module.scss` `.marca`: `display: flex; align-items: center; gap: espaciado(12); color: var(--color-tinta); text-decoration: none; flex-shrink: 0; font-family: var(--fuente-titulares); font-weight: 600; font-size: paso-tipografico(1); letter-spacing: -0.01em; line-height: 1.05` + `@include foco-visible`; `img { @include hueco-de-imagen(1, 1); width: $altura-control-pequena; height: $altura-control-pequena; border-radius: $radio-medio; flex-shrink: 0 }`; `> span { display: flex; flex-direction: column }`; `p { font-family: var(--fuente-texto); font-size: paso-tipografico(-2); font-weight: 600; letter-spacing: 0.16em; text-transform: uppercase; color: var(--color-texto-suave) }`.
   **Tests**: `Cabecera.test.tsx` @s12 sigue verde; añadir test: el enlace `#inicio` contiene una `<img>` con `alt=""` (`queryAllByRole('img')` vacío dentro del enlace, como `PieDePagina.test.tsx:28`), `src` que termina en `/img/logo-galapavet.webp`, `width`/`height` 201, y el descriptor está **dentro** del enlace. Añadir a `Cabecera.test.tsx` (patrón `?raw` ya presente) la lectura del bloque `.marca {` comprobando `@include hueco-de-imagen(1, 1)`.
3. **`src/components/Cabecera.module.scss` (-2, -6, -7, -8, -9) — caja y navegación de escritorio.**
   `.cabecera`: `padding-inline: max(#{espaciado(24)}, calc((100% - #{$ancho-maximo-contenedor}) / 2 + #{espaciado(24)}))`; mantener `background-color: var(--color-fondo)` y añadir `@supports (backdrop-filter: blur(1px)) { background-color: color-mix(in srgb, var(--color-fondo) 88%, transparent); backdrop-filter: blur(#{espaciado(16)}); }`. Mantener `min-height: var(--altura-cabecera)`, `padding-block: espaciado(12)`, `font-size: paso-tipografico(0)`.
   `.navPrincipal ul { gap: espaciado(4) }`; `.navPrincipal a { min-height: $altura-control-pequena; padding-inline: espaciado(8); font-size: paso-tipografico(-1); font-weight: 500 }`; `a[data-enlace-tienda] { @include boton-fantasma; min-height: $altura-control-pequena; padding-inline: espaciado(16); font-size: paso-tipografico(-1); margin-inline-start: espaciado(4) }` (el `@include` primero, literal, para `Cabecera.test.tsx:262-276`).
   **Tests**: ampliar el test `?raw` de `Cabecera.test.tsx` con: `.cabecera {` contiene `$ancho-maximo-contenedor` y `min-height: var(--altura-cabecera)`; el bloque `@supports` contiene `backdrop-filter` y no contiene ningún literal de color (ya lo vigila `puertaLiteralesColor`); ningún control de `.navPrincipal` declara `$altura-control-media` (la causa del bug -6).
   **Medición obligatoria** (navegador real, `bin\harness.ps1 test` + Playwright): a 1024 px `document.querySelector('header').getBoundingClientRect().height` = 65 (64 + borde) y a 1600 px el contenido de la cabecera arranca en `x = 214` como el de las secciones.
4. **`Cabecera.tsx` + `Cabecera.module.scss` (-5, -6) — hamburguesa.** `<button …>` → tres `<span aria-hidden="true" />` + `<span className={styles.soloLectores}>Abrir menú</span>`. `.botonMenu`: quitar `@include boton-fantasma`; `display: flex; flex-direction: column; align-items: center; justify-content: center; gap: espaciado(4); width: $altura-control-pequena; height: $altura-control-pequena; padding: 0; border-radius: $radio-medio; border: $ancho-borde-fino solid var(--color-borde); background-color: var(--color-superficie); color: var(--color-tinta); cursor: pointer` + `foco-visible`; `> span[aria-hidden] { width: 50%; height: math.div(espaciado(8), 4); border-radius: $radio-pequeno; background-color: currentColor }`. `.soloLectores`: `position: absolute; width: 1px; height: 1px; overflow: hidden; clip-path: inset(50%); white-space: nowrap` (sin color: no entra en ninguna puerta; conservar `@media (min-width: 1024px) { display: none }`, el mismo literal).
   **Tests**: los @s3/@s6/@s7/@s8/@s14 existentes siguen verdes (`getByRole('button', { name: 'Abrir menú' })` lee el texto oculto); añadir: el botón no tiene `aria-label`, su `textContent` es "Abrir menú" y contiene exactamente tres hijos `aria-hidden` vacíos.
5. **`Cabecera.module.scss` (-10) — panel móvil.** `.panelMovil`: `padding` del `ul` → `espaciado(16) espaciado(24) espaciado(24)`; `border-block-start: $ancho-borde-fino solid var(--color-borde)`; `a { min-height: $altura-control-media; font-weight: 500; color: var(--color-tinta); &:hover { background-color: var(--color-acento-suave) } }` (dentro de `no-preference` la transición); `a[data-enlace-tienda]` conserva `@include boton-fantasma` y sobrescribe `min-height: $altura-control-media; justify-content: flex-start` para alinear con el resto.
   **Tests**: `?raw` en `Cabecera.test.tsx`: el bloque `.panelMovil {` declara `$altura-control-media` y `--color-tinta`; el `@include boton-fantasma` sigue dentro de `a[data-enlace-tienda] {`.
6. **Verificación de fase 1**: `bin\harness.ps1 test`, `npm run build`, Playwright completo (en especial `accesibilidad.spec.ts:389-430`, `geometria-escalas.spec.ts`, `imagenes.spec.ts`, `css-presupuesto.spec.ts`, `urgencias.spec.ts`), `bin\harness.ps1 mutate`. Anotar en `progress/tdd_rediseno_visual.md` las medidas reales de la cabecera a 1024/1023/320/1600 y los bytes de CSS.

### Fase 2 — tras la enmienda de `cabecera_y_navegacion` @s7 y @s13 (puerta humana)

7. **`src/components/Cabecera-logica.ts` — lógica nueva, mordible por mutación.** Añadir:
   ```ts
   export interface ControlDeUrgencias { readonly rotulo: string; readonly textoVisible: string; readonly enlace: string; readonly textoCta: string }
   /** null si la fuente única no declara rótulo: la cabecera no pinta ningún control de urgencias sin rótulo real (falla cerrado). */
   export function construirControlDeUrgencias(telefono: { rotulo?: string; textoVisible: string; enlaceLlamada: string }): ControlDeUrgencias | null
   ```
   con `textoCta = ${rotulo} · ${textoVisible}` (plantilla) y `enlace = telefono.enlaceLlamada` (nunca `'tel:' + …` retipeado).
   **Tests** (`Cabecera-logica.test.ts`): con rótulo → los cuatro campos con literales escritos a mano ("Urgencias fuera de horario · 91 851 13 93"); sin rótulo → `null`; el `enlace` es exactamente el `enlaceLlamada` recibido; cambiar el teléfono de entrada cambia `textoCta` (mata el mutante de cadena constante).
8. **`Cabecera.tsx` (-3, -15, -16) — píldora de escritorio.** Tras el `<nav>` (hermana, solo en la rama `!movil` y solo si `construirControlDeUrgencias(datosNegocio.telefonoUrgencias) !== null`): `<a href={control.enlace} className={styles.urgencias}>` → `<span aria-hidden="true" />` (punto) + `<span className={styles.soloLectores}>{control.rotulo} </span>` + `{control.textoVisible}`. **Variante A** (recomendada, cabe a 1024). Si el humano elige la **variante B** en la enmienda (texto visible = rótulo, `href="#contacto"`), el mismo hueco cambia de contenido y hay que resolver antes el conflicto 4.
   `Cabecera.module.scss` `.urgencias`: `@include foco-visible; display: inline-flex; align-items: center; gap: espaciado(8); min-height: $altura-control-pequena; padding-inline: espaciado(16); margin-inline-start: espaciado(8); border-radius: $radio-completo; background-color: var(--color-urgencia); color: var(--color-sobre-primario); font-size: paso-tipografico(-1); font-weight: 700; white-space: nowrap; text-decoration: none; flex-shrink: 0`; punto `width/height: espaciado(8); border-radius: $radio-circulo; background-color: currentColor` con `animation: pulso …` dentro de `no-preference` (declarar `@keyframes pulso` en este módulo: los CSS Modules no comparten keyframes entre ficheros); hover `text-decoration: underline; box-shadow: var(--sombra-reposo)`; `&:focus-visible { outline-color: var(--color-sobre-primario) }` como en la barra. No hace falta `@media`: la rama la decide React.
   **Tests** (`Cabecera.test.tsx`, reescritura de @s13 según la enmienda): en escritorio existe exactamente un `link` cuyo nombre accesible contiene el rótulo y el teléfono, con `href` = `datosNegocio.telefonoUrgencias.enlaceLlamada`, **fuera** de la región "Navegación principal" (`within(nav).queryByRole('link', { name: /Urgencias/ })` es `null`); la región sigue con 8 enlaces (@s4 intacto); el texto de la cabecera no contiene "24 h" ni "640 22 11 90"; en móvil (panel cerrado) no existe ese enlace; con un teléfono sin rótulo (prop nueva `telefonoUrgencias` con `rotulo` ausente, mismo patrón de inyección que `Hero.tsx`) no se pinta nada. `?raw`: el bloque `.urgencias {` declara `--color-urgencia` y `--color-sobre-primario` y no declara `filter`.
9. **`Cabecera.tsx` (-11) — CTA del panel móvil.** Tras el `<ul>` del panel: `<a href={control.enlace} className={styles.ctaUrgencias} onClick={() => setAbierto(false)}>{control.textoCta}</a>`. Estilos: `@include foco-visible; display: flex; align-items: center; justify-content: center; min-height: $altura-control-media; margin: espaciado(8) espaciado(24) espaciado(24); padding: espaciado(12) espaciado(16); border-radius: $radio-completo; background-color: var(--color-urgencia); color: var(--color-sobre-primario); font-weight: 700; text-align: center; text-wrap: balance; text-decoration: none`.
   **Tests** (`Cabecera.test.tsx` @s7 reescrito): el panel contiene 8 enlaces de navegación en orden y, después, exactamente un enlace con `href` = `enlaceLlamada` cuyo texto es `${rotulo} · ${textoVisible}`; pulsarlo cierra el panel (`aria-expanded` vuelve a `"false"`); `accesibilidad-teclado` @s25 (Escape) intacto.
10. **Contrato y trazabilidad.** Actualizar `features/cabecera_y_navegacion.feature` (@s7, @s13 y la nota 2 de la cabecera del fichero), `src/data/navegacion.ts:1-10` (el comentario que hoy afirma que el control vive "sin texto visible, ver Cabecera.tsx" es falso) y `progress/rediseno/enmiendas_contrato.md`. Repetir el paso 6 completo; `judge` y `mutation_tester` antes de marcar nada.

### Riesgos a medir (no resueltos en este informe)

- **Anchura a 1024 px con la píldora** (`accesibilidad.spec.ts:401`). Estimación con la variante A y la densidad de -8/-9: logotipo ~250 px (el descriptor real es largo) + navegación ~510 px + píldora ~145 px + Tienda ~80 px + rellenos 64 px ≈ 1.050 px. Puede sobrar o faltar 30-40 px según el render real de DM Sans. Palancas, por orden: `padding-inline: espaciado(8)` en los enlaces (ya en el plan), `letter-spacing: 0.12em` en el descriptor, marca de 32 px (`espaciado(32)`). Si aun así no cabe: conflicto 4 (enmienda del punto de corte), nunca un segundo `@media` en `Cabecera.module.scss`.
- **Barra a 320 px.** "● Urgencias fuera de horario · 91 851 13 93" a 12,8 px/700 mide ~300 px y el ancho útil es 288 px: **probablemente ya hoy pasa a dos líneas** y la barra crece por encima de los 32 px que `--altura-barra-urgencias` reserva, con la cabecera (`inset-block-start: 32px`) tapando la segunda línea. No hay captura móvil para confirmarlo: medir `aside.getBoundingClientRect().height` a 320 px. Si crece, la corrección no es de este componente: `--altura-barra-urgencias` en `global.scss` tendría que pasar a dos pasos con un `@media`, decisión que hay que llevar al humano porque introduce un segundo punto de corte en el proyecto.
- **Techo de CSS** (8.000 B): margen de 480 B en gzip. Reutilizar mixins, no duplicar el `@keyframes` más de una vez por módulo, evitar reglas redundantes. Si se supera, es el humano quien decide si el techo sube (`css-presupuesto.spec.ts:21`, "escrito a mano").
- **Contraste sobre la cabecera translúcida.** La matriz (@s11) valida `--color-texto` sobre `--color-fondo` opaco; con el 88 % + desenfoque sobre la fotografía del hero el ratio real baja unas décimas. `--color-texto` da > 7 sobre `--color-fondo` en `clinica`; medir en `tech` (fondo oscuro sobre foto clara) con `src/lib/contraste.ts` antes de cerrar.
- **`aria-current` + hover en 12,8 px/500.** Comprobar en axe que el enlace de subpágina activa (`--color-primario` sobre fondo) sigue ≥ 4,5 en las cinco variantes (hoy pasa con 16 px/600; el tamaño no cambia el umbral de texto normal).
